---
phase: 03-cover-letter-generation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - client/src/pages/CoverLetter.jsx
  - client/src/pages/CoverLetter.module.css
  - client/src/main.jsx
  - client/src/components/Navbar/Navbar.jsx
autonomous: true
requirements:
  - COVER-01
  - COVER-04
user_setup: []

must_haves:
  truths:
    - "CoverLetter page is accessible at /cover-letter route"
    - "Page displays a dropdown to select from saved job postings"
    - "Generate button triggers POST /api/generate-cover-letter"
    - "Generated paragraph is displayed in a styled text area"
    - "Copy to clipboard button copies the paragraph"
    - "Success/error states are clearly communicated to the user"
    - "Navbar includes a link to the Cover Letter page"
  artifacts:
    - "client/src/pages/CoverLetter.jsx (cover letter generation page)"
    - "client/src/pages/CoverLetter.module.css (page styles)"
    - "client/src/main.jsx (route added)"
    - "client/src/components/Navbar/Navbar.jsx (nav link added)"
  key_links:
    - "GET /api/job-postings provides the list of postings to select from"
    - "POST /api/generate-cover-letter with { job_posting_id } returns the paragraph"
    - "navigator.clipboard.writeText() for copy functionality"
---

<objective>
Build the cover letter generation UI so users can select a saved job posting, generate a tailored cover letter paragraph, and copy it to the clipboard. This is the user-facing component that connects the backend engine (Plan 01) to the user's workflow.

Purpose: Give users a simple interface to generate and use cover letters without leaving the web app.

Output: A functional CoverLetter page at /cover-letter with posting selection, generation, display, and copy-to-clipboard.
</objective>

<execution_context>
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/workflows/execute-plan.md
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@client/src/main.jsx
@client/src/App.jsx
@client/src/components/Navbar/Navbar.jsx
@client/src/pages/NewApplication.jsx
@client/src/pages/NewApplication.module.css
@client/src/index.css
@job_postings.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 3.3: Cover Letter Page — UI for selecting, generating, and copying cover letters</name>
  <files>
    client/src/pages/CoverLetter.jsx,
    client/src/pages/CoverLetter.module.css,
    client/src/main.jsx,
    client/src/components/Navbar/Navbar.jsx
  </files>
  <read_first>
    client/src/main.jsx,
    client/src/App.jsx,
    client/src/components/Navbar/Navbar.jsx,
    client/src/pages/NewApplication.jsx,
    client/src/pages/NewApplication.module.css,
    client/src/index.css,
    job_postings.json
  </read_first>
  <behavior>
    - Test: CoverLetter page renders at /cover-letter
    - Test: Page shows a dropdown populated with saved job postings (company + role)
    - Test: Selecting a posting and clicking Generate sends POST /api/generate-cover-letter
    - Test: Generated paragraph is displayed in a styled area below the form
    - Test: Copy button copies the paragraph to clipboard
    - Test: Loading state shows while generation is in progress
    - Test: Error message shows if generation fails
    - Test: Empty state shows when no job postings exist
    - Test: Navbar has a "Cover Letter" link
  </behavior>
  <action>
    Build the cover letter generation page and wire it into the app navigation.

    **1. Add route** (client/src/main.jsx):
    - Import CoverLetter from './pages/CoverLetter.jsx'
    - Add route: `{ path: 'cover-letter', element: <CoverLetter /> }`
    - Place it after the 'applications' route in the children array

    **2. Add nav link** (client/src/components/Navbar/Navbar.jsx):
    - Add a "Cover Letter" link to `/cover-letter` in the navigation
    - Follow the existing link pattern and styling

    **3. Create page** (client/src/pages/CoverLetter.jsx):

    State:
    - postings: array of job postings (fetched on mount)
    - selectedPostingId: string (selected posting ID)
    - coverLetter: string (generated paragraph)
    - loading: boolean
    - error: string
    - copied: boolean (for copy feedback)

    On mount:
    - Fetch GET /api/job-postings and store in postings state
    - If no postings, show empty state message

    UI sections:
    - Page title: "Cover Letter Generator"
    - Description: "Select a job posting and generate a tailored cover letter paragraph based on your resume."
    - Dropdown: Select a job posting (shows "Company — Role" for each)
      - Default option: "Select a job posting..."
    - Generate button: "Generate Cover Letter"
      - Disabled when no posting selected or loading
      - Shows "Generating..." while loading
    - Result area (shown after generation):
      - Paragraph text in a styled container
      - Copy to Clipboard button
        - Shows "Copied!" for 2 seconds after clicking
      - Character count and sentence count info

    On generate:
    - POST /api/generate-cover-letter with { job_posting_id: selectedPostingId }
    - On success: set coverLetter to response paragraph
    - On error: set error message
    - Clear previous result when generating new one

    On copy:
    - Use navigator.clipboard.writeText(coverLetter)
    - Set copied=true, reset after 2 seconds

    **4. Create styles** (client/src/pages/CoverLetter.module.css):

    Follow existing patterns from NewApplication.module.css:
    - .page: max-width 800px, margin 0 auto, padding 2rem
    - .description: color muted, margin-bottom 1.5rem
    - .form: display flex, flex-direction column, gap 1.5rem
    - .select: padding 0.5rem, border 1px solid #e0e0e0, border-radius 4px, font-size 1rem
    - .generateButton: primary color background, white text, padding 0.75rem 2rem
    - .generateButton:disabled: opacity 0.6, cursor not-allowed
    - .result: margin-top 2rem, background #f9fafb, border-radius 8px, padding 1.5rem
    - .paragraph: line-height 1.7, font-size 1rem, white-space pre-wrap
    - .resultActions: display flex, gap 1rem, align-items center, margin-top 1rem
    - .copyButton: secondary style (outline button)
    - .copied: color green, font-size 0.875rem
    - .meta: font-size 0.75rem, color muted, margin-top 0.5rem
    - .error: color #ef4444, font-size 0.875rem
    - .empty: text-align center, padding 3rem, color muted

    Empty state:
    - Show "No job postings found. Add a job posting first." with a link to /new

    **After implementing:**
    - Navigate to /cover-letter
    - Select a posting from the dropdown
    - Click Generate Cover Letter
    - Verify paragraph appears with company name and role
    - Click Copy to Clipboard
    - Verify "Copied!" feedback appears
  </action>
  <verify>
    <automated>
      cd /Users/kmpg/VibeCodeTours/applytrail && node -e "
        const http = require('http');

        // Test 1: Page loads (check that the route serves HTML)
        http.get('http://localhost:5173/cover-letter', (res) => {
          let d = '';
          res.on('data', c => d += c);
          res.on('end', () => {
            // Vite serves index.html for all SPA routes
            if (!d.includes('<div id=\"root\"')) throw new Error('Page did not serve SPA shell');
            console.log('GET /cover-letter OK: SPA shell served');
          });
        });

        // Test 2: API endpoint exists and works
        const postings = require('./job_postings.json');
        if (postings.length === 0) {
          console.log('SKIP: No job postings to test with');
          return;
        }
        const job = postings[0];
        const postData = JSON.stringify({ job_posting_id: job.id });
        const req = http.request({ hostname: 'localhost', port: 3000, path: '/api/generate-cover-letter', method: 'POST', headers: { 'Content-Type': 'application/json', 'Content-Length': Buffer.byteLength(postData) } }, (res) => {
          let d = '';
          res.on('data', c => d += c);
          res.on('end', () => {
            const r = JSON.parse(d);
            if (!r.ok || !r.cover_letter_paragraph) throw new Error('API failed: ' + d);
            console.log('API OK: paragraph length=' + r.cover_letter_paragraph.length);
            console.log('First 100 chars: ' + r.cover_letter_paragraph.substring(0, 100));
          });
        });
        req.write(postData);
        req.end();
      "
    </automated>
  </verify>
  <done>
    - CoverLetter page accessible at /cover-letter
    - Dropdown shows saved job postings with company and role
    - Generate button triggers API call and displays paragraph
    - Copy to clipboard works with "Copied!" feedback
    - Loading state shown during generation
    - Error state shown if generation fails
    - Empty state shown when no postings exist, with link to /new
    - Navbar includes Cover Letter link
    - Route added to main.jsx
    - Styling matches app design system
  </done>
</task>

</tasks>

<verification>
Phase-level verification checks:

1. **Route works:** Navigate to /cover-letter in browser — page loads
2. **Dropdown populated:** Select shows job postings from job_postings.json
3. **Generation works:** Select a posting, click Generate — paragraph appears
4. **Paragraph quality:** Contains company name, role, 4-6 sentences
5. **Copy works:** Click Copy — clipboard contains paragraph text
6. **Empty state:** If no postings exist, shows helpful message with link to /new
7. **Nav link:** Navbar has Cover Letter link that navigates to /cover-letter
</verification>

<success_criteria>
1. User can select a job posting from a dropdown
2. Clicking Generate produces a tailored cover letter paragraph
3. Paragraph mentions the target role and company name
4. Paragraph includes matched resume experience
5. User can copy the paragraph to clipboard with one click
6. Empty state guides user to add a job posting first
7. Page is accessible from the main navigation
</success_criteria>

<artifacts_this_phase_produces>
- client/src/pages/CoverLetter.jsx — Cover letter generation page
- client/src/pages/CoverLetter.module.css — Page styles
- client/src/main.jsx — Updated with /cover-letter route
- client/src/components/Navbar/Navbar.jsx — Updated with Cover Letter nav link
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/03-cover-letter-generation/03-02-SUMMARY.md` when done
</output>
