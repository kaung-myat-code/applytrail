---
phase: 13-application-pre-fill-and-export
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - client/src/components/CreateApplicationModal.jsx
  - client/src/components/CreateApplicationModal.module.css
autonomous: true
requirements: [PREFILL-01, PREFILL-02, PREFILL-03]

must_haves:
  truths:
    - "The modal displays company, role, a truncated (~280 char) job posting excerpt, and the linked resume version's name, pre-filled from props passed by the caller (per D-03)"
    - "Company, role, and status are editable inline; job posting excerpt and resume version name are read-only (per D-04)"
    - "On open, the modal auto-generates a cover letter paragraph by calling POST /api/generate-cover-letter with the posting id, showing a loading state then the editable result (per D-05); a failed generation leaves the field empty and editable with a non-blocking inline note, and does not prevent Confirm"
    - "Confirm calls POST /api/applications with job_posting_id, resume_version_id, company, role, status, and cover_letter_paragraph, and only closes/succeeds on a 200 ok response; Cancel dismisses without any POST /api/applications call"
    - "Default status on open is 'drafted' (per D-06), selectable from the same VALID_STATUSES set used elsewhere in the app"
    - "Escape key, backdrop click, and the Cancel button all trigger the same dismiss behavior (no unsaved-changes warning)"
    - "UI Considerations lift (covered, loading): cover letter field shows 'Generating cover letter...' while the auto-generation request is in flight; Confirm remains enabled during this wait"
    - "UI Considerations lift (covered, error): cover letter auto-generation failure falls back to an empty editable field plus the inline note, without blocking Confirm"
    - "UI Considerations lift (covered, error): a failed POST /api/applications keeps the modal open with an inline error and all field values preserved for retry"
    - "UI Considerations lift (covered, overflow): job posting excerpt over ~280 characters is truncated with a trailing ellipsis to prevent an unbounded modal height"
    - "UI Considerations lift (covered, long-text): Company/Role inputs accept unrestricted text length with standard browser input behavior (no character limit enforced)"
    - "UI Considerations lift (covered, loading): the Confirm button disables and swaps to 'Creating...' while POST /api/applications is in flight, preventing duplicate submission"
    - "UI Considerations lift (covered, populated): the happy-path render shows company, role, job posting excerpt, resume version name, and generated cover letter all pre-filled simultaneously"
    - "UI Considerations lift (backstop, empty): { statement: \"When postingText is empty or missing, the read-only Job Posting field shows the literal text 'No job posting text available.' rather than blank space\", verification: backstop }"
    - "UI Considerations lift (backstop, overflow): { statement: \"A very long resume version name in the read-only Resume Version display is truncated with CSS text-overflow: ellipsis / white-space: nowrap / overflow: hidden rather than wrapping or overflowing the dialog\", verification: backstop }"
    - "UI Considerations lift (backstop, partial): { statement: \"If the job posting referenced by postingId no longer exists by the time the modal is shown, the modal degrades to the same empty-posting-text backstop ('No job posting text available.') rather than throwing or leaving the field in a broken state\", verification: backstop }"
  artifacts:
    - client/src/components/CreateApplicationModal.jsx
    - client/src/components/CreateApplicationModal.module.css
  key_links:
    - "CreateApplicationModal mount -> useEffect -> POST /api/generate-cover-letter({ job_posting_id: postingId }) -> setCoverLetter(data.cover_letter_paragraph) | setCoverLetterError(true) on failure"
    - "CreateApplicationModal Confirm -> POST /api/applications({ job_posting_id, resume_version_id, company, role, status, cover_letter_paragraph }) -> onSuccess(data.application) callback to caller"
  prohibitions:
    - "The modal must not call POST /api/applications automatically on mount or on cover-letter-load -- creation must only ever be triggered by an explicit user Confirm click"
    - "The modal must not block Confirm while the cover letter is still loading or failed to generate -- an empty/partial cover letter must not prevent application creation, per D-05 discretion note ('user may confirm with an empty/partial cover letter')"
    - "The modal must not mutate or re-fetch the resume version or job posting it was given -- all resume/posting data arrives via props from the caller (PreviewTailored.jsx or ResumeLibrary.jsx in plan 13-03), this component only reads them"
</must_haves>
---

<objective>
Build the first modal/dialog component in the codebase: a confirmation dialog that pre-fills company, role, job posting excerpt, and linked resume version name, auto-generates a cover letter, and lets the user edit company/role/status/cover-letter before confirming creation of a new application record.

Purpose: This is the reusable UI building block both trigger points (auto-trigger from PreviewTailored.jsx, manual-trigger from ResumeLibrary.jsx) will mount in plan 13-03. Building it standalone first, against the stable API contract from plan 13-01, keeps this plan's scope to a single concern.
Output: `CreateApplicationModal.jsx` + `CreateApplicationModal.module.css`, fully functional in isolation (accepts props, calls the two existing/extended endpoints, calls back to its parent on success/cancel).
</objective>

<execution_context>
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/workflows/execute-plan.md
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-application-pre-fill-and-export/13-CONTEXT.md
@.planning/phases/13-application-pre-fill-and-export/13-PATTERNS.md
@.planning/phases/13-application-pre-fill-and-export/13-UI-SPEC.md
@client/src/pages/PreviewTailored.jsx
@client/src/pages/ResumeLibrary.jsx
@client/src/pages/ResumeLibrary.module.css
@client/src/pages/Applications.jsx
@client/src/index.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build CreateApplicationModal component with cover-letter auto-generation</name>
  <files>client/src/components/CreateApplicationModal.jsx</files>
  <read_first>
    client/src/pages/PreviewTailored.jsx (fetch-on-mount useEffect pattern, lines 21-49; handleSave try/catch/finally pattern, lines 51-69)
    client/src/pages/ResumeLibrary.jsx (renamingId/renameValue controlled-input pattern, lines 10-11, 142-153)
    client/src/pages/Applications.jsx (STATUS_OPTIONS array, lines 5, and status select usage)
    .planning/phases/13-application-pre-fill-and-export/13-UI-SPEC.md (section "Modal Interaction Contract" — authoritative structure; section "Copywriting Contract" — authoritative text)
  </read_first>
  <behavior>
    - Given props `{ mode: 'auto' | 'manual', company, role, postingText, postingId, resumeVersionId, resumeVersionName, onCancel, onSuccess }`: on mount, the modal renders Company/Role inputs pre-filled from props, a read-only Job Posting excerpt (truncated to 280 chars + "…" if longer, or "No job posting text available." if `postingText` is empty/undefined), a read-only Resume Version display showing `resumeVersionName`, a Status select defaulting to `'drafted'`, and a Cover Letter textarea showing "Generating cover letter..." while a `POST /api/generate-cover-letter` call (body `{ job_posting_id: postingId }`) is in flight
    - Given the cover-letter fetch resolves successfully: the textarea populates with `data.cover_letter_paragraph` and becomes user-editable; given the fetch fails (network error or non-ok response): the textarea is empty and editable, and an inline note "Couldn't auto-generate a cover letter. You can write one manually below, or leave it blank and add it later." appears below it — Confirm remains enabled in both outcomes
    - Given the user clicks "Confirm & Create Application" (button label swaps to "Creating..." and disables during the request): a `POST /api/applications` call fires with `{ job_posting_id: postingId, resume_version_id: resumeVersionId, company, role, status, cover_letter_paragraph }` (current field values, reflecting any edits) — on success (`res.ok` true), `onSuccess(data.application)` is called; on failure, an inline error "Couldn't create the application. {server error message}. Try again." appears and the modal stays open with all field values preserved
    - Given the user clicks Cancel, presses Escape, or clicks the backdrop: `onCancel()` is called immediately with no `POST /api/applications` call ever fired
    - Given `mode === 'auto'`: modal title is "Create application for this resume?"; given `mode === 'manual'`: modal title is "Create Application" (both from UI-SPEC.md Copywriting Contract)
  </behavior>
  <action>
    Create `client/src/components/CreateApplicationModal.jsx` as a function component `CreateApplicationModal({ mode, company, role, postingText, postingId, resumeVersionId, resumeVersionName, onCancel, onSuccess })`.

    State: `companyValue`/`roleValue` (initialized from `company`/`role` props, controlled inputs — mirror the `renameValue` controlled-input pattern from ResumeLibrary.jsx), `status` (initialized `'drafted'`), `coverLetter` (initialized `''`), `coverLetterLoading` (initialized `true`), `coverLetterError` (initialized `false`), `submitting` (initialized `false`), `submitError` (initialized `''`).

    On mount (`useEffect(() => {...}, [])`), call `POST /api/generate-cover-letter` with body `{ job_posting_id: postingId }` following the exact fetch/try/catch/finally shape from `PreviewTailored.jsx`'s `handleSave` — on success set `coverLetter` from `data.cover_letter_paragraph`, on any thrown error set `coverLetterError = true` and leave `coverLetter` as `''`, always set `coverLetterLoading = false` in `finally`.

    Import `VALID_STATUSES`-equivalent inline as a local const array `['drafted', 'applied', 'interviewing', 'offered', 'rejected', 'withdrawn']` (matching the `STATUS_OPTIONS` array already defined in `client/src/pages/Applications.jsx` line 5 — duplicate the literal here rather than importing across page/component boundary, consistent with how this codebase currently has no shared-constants module for this list) for the Status `<select>` options.

    Job posting excerpt: compute `const excerpt = !postingText ? 'No job posting text available.' : (postingText.length > 280 ? postingText.slice(0, 280) + '…' : postingText)` and render read-only (plain text or a disabled/readOnly textarea — a `<p>` or `<div>` is sufficient since it's non-interactive).

    Resume version display: render `resumeVersionName` read-only, applying `text-overflow: ellipsis; white-space: nowrap; overflow: hidden` via the CSS module (per UI-SPEC.md backstop for long resume-version names).

    `handleConfirm` async function: guard `if (submitting) return`, set `submitting = true`, clear `submitError`, POST to `/api/applications` with `{ job_posting_id: postingId, resume_version_id: resumeVersionId, company: companyValue, role: roleValue, status, cover_letter_paragraph: coverLetter }`, parse JSON, `if (!res.ok) throw new Error(data.error || 'Couldn't create the application. Try again.')`, on success call `onSuccess(data.application)`; catch sets `submitError` to the UI-SPEC.md copy template `` `Couldn't create the application. ${err.message}. Try again.` ``; finally sets `submitting = false`.

    Dismiss handling: attach an `onKeyDown` listener (or a `useEffect` with `document.addEventListener('keydown', ...)`, cleaned up on unmount) that calls `onCancel()` when `e.key === 'Escape'`. The backdrop `<div>` (outermost element, full-viewport) gets `onClick={onCancel}`; the dialog surface `<div>` inside it must call `e.stopPropagation()` on its own onClick to prevent backdrop-click-through when clicking inside the dialog. Cancel button calls `onCancel()` directly.

    Focus management: on mount, focus the Company input (`useRef` + `useEffect(() => { companyInputRef.current?.focus() }, [])`) per UI-SPEC.md's Modal Interaction Contract ("Focus moves to the first editable field (Company input) on open").

    Scroll lock: in a `useEffect` on mount, set `document.body.style.overflow = 'hidden'`, and return a cleanup function resetting it to `''` on unmount (per UI-SPEC.md "Scroll behavior").

    Title: `mode === 'auto' ? 'Create application for this resume?' : 'Create Application'`. Subtext: "Review the details below, then confirm to add this to your application tracker." (verbatim from UI-SPEC.md Copywriting Contract). Field labels verbatim from UI-SPEC.md: "Company", "Role", "Status", "Job Posting", "Resume Version", "Cover Letter (editable)". Primary CTA label: "Confirm & Create Application" (submitting: "Creating..."). Secondary action label: "Cancel".

    Apply CSS module classes from `CreateApplicationModal.module.css` (built in Task 2 of this plan) for backdrop, dialog, field groups, and buttons — reuse the `.btn`/`.btnPrimary`-equivalent visual language already established in `ResumeLibrary.module.css` (do not invent new visual tokens; the CSS module in Task 2 defines local class names but pulls colors/spacing/radii from the same root `--color-*`/`--space-*`/`--radius-*` custom properties already in `client/src/index.css`).
  </action>
  <acceptance_criteria>
    - Component file exports a default function `CreateApplicationModal` accepting the exact prop names listed above
    - `grep -c "onSuccess\|onCancel" client/src/components/CreateApplicationModal.jsx` is greater than 0 for each, confirming both callback props are invoked somewhere in the component body
    - `grep -n "POST /api/applications\|/api/applications'" client/src/components/CreateApplicationModal.jsx` shows the fetch call only inside `handleConfirm`, not inside the mount `useEffect`
    - `grep -n "'drafted'" client/src/components/CreateApplicationModal.jsx` shows it as the initial `status` state value
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s = require('fs').readFileSync('client/src/components/CreateApplicationModal.jsx','utf8'); const checks = [[/function CreateApplicationModal/, 'component fn'],[/useState\('drafted'\)/, 'default status drafted'],[/generate-cover-letter/, 'cover letter fetch'],[/api\/applications/, 'applications post'],[/Escape/, 'escape key handling'],[/onCancel/, 'onCancel usage'],[/onSuccess/, 'onSuccess usage']]; let fail=false; for (const [re,label] of checks){ if(!re.test(s)){ console.error('MISSING:', label); fail=true; } } if(fail) process.exit(1); console.log('OK all structural checks passed')"</automated>
  </verify>
  <done>CreateApplicationModal.jsx exists, renders pre-filled/editable fields per props, auto-generates a cover letter on mount with graceful failure handling, and only calls POST /api/applications on explicit Confirm.</done>
</task>

<task type="auto">
  <name>Task 2: Style the modal per UI-SPEC.md Modal Interaction Contract</name>
  <files>client/src/components/CreateApplicationModal.module.css</files>
  <read_first>
    .planning/phases/13-application-pre-fill-and-export/13-UI-SPEC.md (sections "Modal Interaction Contract", "Color", "Typography", "Spacing Scale")
    client/src/pages/ResumeLibrary.module.css (fadeInUp keyframe at line 217; .btn/.btnPrimary/.error class definitions to mirror)
    client/src/index.css (root custom properties: --color-*, --space-*, --radius-*, --shadow-*, --font-*)
  </read_first>
  <action>
    Create `client/src/components/CreateApplicationModal.module.css` defining these classes, using only existing root custom properties (no new colors/spacing/fonts introduced, per UI-SPEC.md's "reuse verbatim" rule):

    `.backdrop`: `position: fixed`, `inset: 0` (full viewport), `background: rgba(26, 22, 37, 0.4)` (literal value per UI-SPEC.md Modal Interaction Contract — no existing overlay token exists), `display: flex`, `align-items: center`, `justify-content: center`, `z-index` high enough to sit above page content (e.g. `1000`), with a simple opacity-fade-in animation.

    `.dialog`: `background: var(--color-surface)`, `border-radius: var(--radius-lg)`, `box-shadow: var(--shadow-lg)`, `max-width: 560px`, `width: 90%`, `max-height: 90vh`, `overflow-y: auto`, `padding: var(--space-5)`, animation using the existing `fadeInUp` keyframe (copy the exact `@keyframes fadeInUp { from { opacity: 0; transform: translateY(4px); } to { opacity: 1; transform: translateY(0); } }` definition verbatim from `ResumeLibrary.module.css` line 217 into this file, since CSS Modules do not share keyframes across files).

    `.title`: `font-family: var(--font-body)`, `font-size: 1.25rem`, `font-weight: 600`, `margin-bottom: var(--space-2)` (per UI-SPEC.md Typography table: "Modal title (H2)" role).

    `.subtext`: `color: var(--color-muted)`, `font-size: 0.9rem`, `margin-bottom: var(--space-5)`.

    `.field`: `margin-bottom: var(--space-4)` wrapper for each label+input pair. `.label`: `font-size: 0.85rem`, `font-weight: 600`, `display: block`, `margin-bottom: var(--space-2)`.

    `.input`, `.select`, `.textarea`: reuse the exact sizing/border/focus pattern from `.renameInput` in `ResumeLibrary.module.css` (`padding: var(--space-2) var(--space-3)`, `border: 1px solid var(--color-border)`, `border-radius: var(--radius-sm)`, `font-family: var(--font-body)`, `font-size: 0.9rem`, `width: 100%`; `:focus` sets `border-color: var(--color-primary)` and `box-shadow: var(--shadow-focus)`). `.textarea` additionally sets `min-height: 100px`, `resize: vertical`.

    `.readOnlyField`: `color: var(--color-text-secondary)`, `font-size: 0.9rem`, `padding: var(--space-2) 0`.

    `.resumeVersionName`: `text-overflow: ellipsis`, `white-space: nowrap`, `overflow: hidden`, `display: block`, `max-width: 100%` (per UI-SPEC.md backstop for long names).

    `.coverLetterNote`: `font-size: 0.8rem`, `color: var(--color-muted)`, `margin-top: var(--space-2)`.

    `.error`: copy exact definition from `ResumeLibrary.module.css` (`color: var(--color-danger)`, `font-size: 0.85rem`, `margin-bottom: var(--space-4)`, `padding: var(--space-3) var(--space-4)`, `background: var(--color-danger-bg)`, `border-radius: var(--radius-sm)`).

    `.footer`: `display: flex`, `justify-content: flex-end`, `gap: var(--space-3)`, `margin-top: var(--space-5)`, `padding-top: var(--space-4)`, `border-top: 1px solid var(--color-border-light)`.

    `.btnPrimary`, `.btn`: copy exact class bodies from `ResumeLibrary.module.css` lines 91-133 (`.btn` and `.btnPrimary`, including `:hover`/`:disabled` states) verbatim — these are the neutral/primary button styles per UI-SPEC.md's accent-color budget rule ("Confirm & Create Application" uses `.btnPrimary`, "Cancel" uses `.btn`).
  </action>
  <acceptance_criteria>
    - File defines `.backdrop`, `.dialog`, `.title`, `.subtext`, `.field`, `.label`, `.input`, `.select`, `.textarea`, `.readOnlyField`, `.resumeVersionName`, `.coverLetterNote`, `.error`, `.footer`, `.btn`, `.btnPrimary` classes
    - No new CSS custom property is introduced (only `var(--color-*)`, `var(--space-*)`, `var(--radius-*)`, `var(--shadow-*)`, `var(--font-*)` references plus the one literal `rgba(26, 22, 37, 0.4)` backdrop tint documented in UI-SPEC.md)
    - `fadeInUp` keyframe is defined locally in this file (CSS Modules scope keyframes per-file)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s = require('fs').readFileSync('client/src/components/CreateApplicationModal.module.css','utf8'); const classes = ['.backdrop','.dialog','.title','.subtext','.field','.label','.input','.select','.textarea','.readOnlyField','.resumeVersionName','.coverLetterNote','.error','.footer','.btn','.btnPrimary']; let fail=false; for (const c of classes) { if (!s.includes(c)) { console.error('MISSING CLASS:', c); fail=true; } } if (!/@keyframes fadeInUp/.test(s)) { console.error('MISSING keyframes fadeInUp'); fail=true; } if (fail) process.exit(1); console.log('OK all classes present')"</automated>
  </verify>
  <done>CreateApplicationModal.module.css exists and defines every class the component references, using only existing design tokens plus the one documented backdrop rgba literal.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Client component -> Express API | CreateApplicationModal issues fetch() calls to POST /api/generate-cover-letter and POST /api/applications; both are existing/extended endpoints already covered by plan 13-01's threat register |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Tampering | CreateApplicationModal Company/Role inline-edit fields | low | accept | These are plain controlled text inputs sent as-is to POST /api/applications; the server stores them as opaque strings in a local JSON file with no rendering as HTML/script anywhere (React's default text rendering escapes content) — no XSS or injection vector in a single-user local JSON-file tool |
| T-13-06 | Repudiation | Confirm button double-submission | low | mitigate | submitting state guard (`if (submitting) return`) and disabled button during the in-flight request prevent duplicate POST /api/applications calls from a double-click, consistent with the existing disabled+label-swap pattern used elsewhere in this codebase |

No new server-side trust boundary is introduced by this plan — it is a pure frontend component. Authentication is out of scope (single-user local tool, accepted per CLAUDE.md).
</threat_model>

<verification>
- `cd client && npm run build` (or `npm run lint` if configured) completes without errors referencing the new files
- Manual: render `<CreateApplicationModal mode="manual" company="Acme" role="Engineer" postingText="..." postingId="abc123" resumeVersionId="def456" resumeVersionName="Acme - Engineer" onCancel={() => {}} onSuccess={() => {}} />` in isolation (e.g. temporarily mounted in a test route or via React DevTools) and confirm: cover letter loads, fields are editable, Escape/backdrop/Cancel all dismiss without a network call, Confirm posts and calls onSuccess
</verification>

<success_criteria>
- CreateApplicationModal.jsx renders all required fields pre-filled from props, with Company/Role/Status/Cover-Letter editable and Job-Posting-excerpt/Resume-Version read-only
- Cover letter auto-generates on mount with a graceful, non-blocking failure path
- Confirm and only Confirm triggers POST /api/applications; Cancel/Escape/backdrop-click never do
- Styling matches UI-SPEC.md's Modal Interaction Contract using only existing design tokens
</success_criteria>

## Artifacts this phase produces

- `client/src/components/CreateApplicationModal.jsx` — new file, default-exports `CreateApplicationModal({ mode, company, role, postingText, postingId, resumeVersionId, resumeVersionName, onCancel, onSuccess })`
- `client/src/components/CreateApplicationModal.module.css` — new file, CSS Modules classes: `.backdrop`, `.dialog`, `.title`, `.subtext`, `.field`, `.label`, `.input`, `.select`, `.textarea`, `.readOnlyField`, `.resumeVersionName`, `.coverLetterNote`, `.error`, `.footer`, `.btn`, `.btnPrimary`, `@keyframes fadeInUp`

<output>
Create `.planning/phases/13-application-pre-fill-and-export/13-02-SUMMARY.md` when done
</output>
