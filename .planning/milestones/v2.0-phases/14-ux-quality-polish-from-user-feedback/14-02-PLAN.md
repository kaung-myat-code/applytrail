---
phase: 14-ux-quality-polish-from-user-feedback
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - server/lib/analysis/keywords.js
  - server/lib/analysis/keywords.test.js
  - client/src/lib/keywordCasing.js
  - client/src/pages/Analysis.jsx
autonomous: true
requirements: [UX-ISSUE-04]

must_haves:
  truths:
    - "extractKeywords() on the text 'SQL, dashboarding, stakeholder communication, experimentation, product metrics, React, and Python' returns a set that includes sql, dashboarding, 'stakeholder communication', experimentation, 'product metrics', react, and python — not just react and sql as it does today."
    - "TECH_KEYWORDS gains a new product/data/business-soft-skill category without removing or renaming any existing entry (extractKeywords remains backward compatible for every currently-matched technical term)."
    - "server/lib/analysis/keywords.js exports ACRONYM_CASING alongside TECH_KEYWORDS, extractKeywords, and extractResumeKeywords."
    - "The Analysis page's Matched/Missing/Bonus keyword badges and per-section Matched/Missing item badges render 'SQL' (not 'sql') and 'API' (not 'api') for any keyword present in the acronym-casing map, while keywords already containing uppercase letters (e.g. from a future AI provider) are left exactly as returned, never re-capitalized."
  artifacts:
    - path: "client/src/lib/keywordCasing.js"
      provides: "Client-side display-casing helper mirroring server/lib/analysis/keywords.js's ACRONYM_CASING, since client/ and server/ are separate npm packages with no shared module boundary"
      exports: ["ACRONYM_CASING", "displayCase"]
    - path: "server/lib/analysis/keywords.test.js"
      provides: "Regression test proving the whitelist-breadth fix against the exact CONTEXT.md example posting"
      min_lines: 15
  key_links:
    - from: "client/src/pages/Analysis.jsx (KeywordGroups, SectionFindings)"
      to: "client/src/lib/keywordCasing.js"
      via: "import { displayCase } from '../lib/keywordCasing' then displayCase(kw) at every badge render site"
      pattern: "displayCase\\("
    - from: "server/lib/analysis/providers/heuristic.js (Plan 14-03)"
      to: "server/lib/analysis/keywords.js"
      via: "require('../keywords').ACRONYM_CASING, consumed by a future plan's displayCase() helper for generated suggestion text"
      pattern: "ACRONYM_CASING"
  prohibitions:
    - "MUST NOT include keywords in the expanded whitelist that proxy for protected characteristics or coded/biased hiring language (e.g. age-coded terms like 'digital native', gendered trait framing) — only genuine, neutral product/data/business-skill vocabulary may be added."
---

<objective>
Broaden the match-analysis keyword whitelist beyond the current technical-only list to cover product, data-analytics, and business-soft-skill terms (per D-13), and fix the naive-lowercase display bug for known acronyms (e.g. "Sql" → "SQL") via a maintained acronym-exceptions map (per D-15). Closes GitHub #4.

Purpose: The current whitelist reduces a realistic Product Analyst posting mentioning "SQL, dashboarding, stakeholder communication, experimentation, product metrics, React, and Python" down to only "react" and "sql" — most of the posting's actual requirements are invisible to match scoring, gap analysis, and suggestion generation.
Output: `server/lib/analysis/keywords.js` extended with a broadened whitelist, multi-word phrase matching, and an `ACRONYM_CASING` map; a passing regression test; a client-side display-casing utility consumed by the Analysis page's keyword badges.
</objective>

<execution_context>
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/workflows/execute-plan.md
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-ux-quality-polish-from-user-feedback/14-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Broaden TECH_KEYWORDS, fix multi-word phrase matching, add ACRONYM_CASING</name>
  <files>server/lib/analysis/keywords.js, server/lib/analysis/keywords.test.js</files>
  <read_first>
    server/lib/analysis/keywords.js (full file — TECH_KEYWORDS Set structure at lines 14-101, and extractKeywords at lines 111-120; note the tokenizer splits on `/[^a-z0-9.+#-]+/`, which strips spaces, so a multi-word Set member like "stakeholder communication" can never match via the current single-token split-and-filter alone)
    server/lib/pdf.test.js (zero-dependency assert()+test() harness convention to replicate)
    .planning/phases/14-ux-quality-polish-from-user-feedback/14-CONTEXT.md (the "Specific Ideas" section — the exact posting text example that must extract correctly after this fix)
  </read_first>
  <behavior>
    - Test 1: extractKeywords('SQL, dashboarding, stakeholder communication, experimentation, product metrics, React, and Python') returns an array containing all of: sql, dashboarding, 'stakeholder communication', experimentation, 'product metrics', react, python.
    - Test 2: extractKeywords('We use React and TypeScript daily') still returns ['react', 'typescript'] (existing single-word behavior unchanged).
    - Test 3: ACRONYM_CASING.sql === 'SQL' and ACRONYM_CASING.api === 'API'.
  </behavior>
  <action>
    Add a new category to the `TECH_KEYWORDS` Set (after the existing `// --- Methodologies ---` category, following the same lowercase, comma-separated, alphabetically-sorted-within-category convention with a `// --- Product / Data / Business skills ---` header comment) containing at minimum: 'dashboarding', 'dashboards', 'experimentation', 'stakeholder communication', 'product metrics', 'roadmapping', 'prioritization', 'okr', 'okrs', 'kpi', 'kpis', 'roi', 'seo', 'ab-testing', 'cohort-analysis', 'forecasting', 'data-visualization', 'stakeholder-management', 'cross-functional', 'go-to-market', 'product-strategy', 'b2b', 'b2c'. Two of these ('stakeholder communication', 'product metrics') are deliberately multi-word — this requires a functional change to `extractKeywords`, not just a Set addition: after the existing single-token `tokens` extraction (unchanged), add a second pass that checks each `TECH_KEYWORDS` member containing a space against `lower.includes(member)` (where `lower = text.toLowerCase()`, computed once and reused for both passes) and includes any match. Merge both passes through the existing `new Set([...])` dedupe before returning, so the function's return shape and single-word behavior are unchanged.
    Add a new `const ACRONYM_CASING = { sql: 'SQL', api: 'API', apis: 'APIs', aws: 'AWS', gcp: 'GCP', css: 'CSS', html: 'HTML', ui: 'UI', ux: 'UX', ci: 'CI', cd: 'CD', tdd: 'TDD', bdd: 'BDD', php: 'PHP', rest: 'REST', restful: 'RESTful', grpc: 'gRPC', graphql: 'GraphQL', json: 'JSON', xml: 'XML', yaml: 'YAML', jwt: 'JWT', oauth: 'OAuth', saml: 'SAML', ldap: 'LDAP', kpi: 'KPI', kpis: 'KPIs', okr: 'OKR', okrs: 'OKRs', roi: 'ROI', seo: 'SEO', b2b: 'B2B', b2c: 'B2C', nosql: 'NoSQL', dotnet: '.NET' }` placed directly below `TECH_KEYWORDS`. Update the final `module.exports` line to add `ACRONYM_CASING` to the existing `{ TECH_KEYWORDS, extractKeywords, extractResumeKeywords }` export.
    Create server/lib/analysis/keywords.test.js following the pdf.test.js `assert`+`test()` harness convention, covering the three behaviors above plus a fourth test asserting the module exports all four names (`TECH_KEYWORDS`, `extractKeywords`, `extractResumeKeywords`, `ACRONYM_CASING`).
  </action>
  <acceptance_criteria>
    - `extractKeywords('SQL, dashboarding, stakeholder communication, experimentation, product metrics, React, and Python')` includes all 7 terms from the CONTEXT.md example (case-insensitively, as lowercase tokens except the two multi-word phrases).
    - `extractKeywords('We use React and TypeScript daily')` still returns exactly `['react', 'typescript']` (no regression to existing single-word matching).
    - `server/lib/analysis/keywords.js` exports `ACRONYM_CASING` with `ACRONYM_CASING.sql === 'SQL'`.
    - `node server/lib/analysis/keywords.test.js` exits 0 with no `FAIL:` lines.
  </acceptance_criteria>
  <verify>
    <automated>node server/lib/analysis/keywords.test.js</automated>
  </verify>
  <done>extractKeywords correctly extracts every term in the CONTEXT.md example posting, existing single-word extraction is unchanged, and ACRONYM_CASING is exported.</done>
</task>

<task type="auto">
  <name>Task 2: Client-side display casing for Analysis page keyword badges</name>
  <files>client/src/lib/keywordCasing.js, client/src/pages/Analysis.jsx</files>
  <read_first>
    client/src/pages/Analysis.jsx (KeywordGroups at lines 43-87 and SectionFindings at lines 89-150 — both currently render raw lowercase `kw`/`item` strings directly as badge text)
    server/lib/analysis/keywords.js (the ACRONYM_CASING map just created in Task 1 — client/ and server/ are separate npm packages, so this map must be duplicated, not imported, across the package boundary)
  </read_first>
  <action>
    Create client/src/lib/keywordCasing.js exporting the identical `ACRONYM_CASING` object literal from server/lib/analysis/keywords.js Task 1 (same keys and values, byte-for-byte), plus a `displayCase(kw)` function: return `ACRONYM_CASING[kw]` if present; otherwise, if `kw === kw.toLowerCase()` (the keyword is fully lowercase, which is always true for heuristic-provider output), return `kw.charAt(0).toUpperCase() + kw.slice(1)`; otherwise return `kw` unchanged (never re-capitalize a keyword that already contains uppercase letters, which protects against corrupting a future AI-provider's already-correctly-cased keyword strings). Add a one-line comment above `ACRONYM_CASING` noting it must be kept in sync with `server/lib/analysis/keywords.js`'s `ACRONYM_CASING`.
    In client/src/pages/Analysis.jsx, add `import { displayCase } from '../lib/keywordCasing'` near the top. In `KeywordGroups`, wrap the badge text at all three render sites (matched.map, missing.map, bonus.map — the `{kw}` interpolations inside the `<span className={styles.keywordBadge}...>` elements) with `{displayCase(kw)}`. In `SectionFindings`, wrap the badge text at both render sites (`section.matchedItems.map`, `section.missingItems.map` — the `{item}` interpolations) with `{displayCase(item)}`.
  </action>
  <acceptance_criteria>
    - `client/src/lib/keywordCasing.js` exports `ACRONYM_CASING` and `displayCase`.
    - `displayCase('sql') === 'SQL'`, `displayCase('dashboarding') === 'Dashboarding'`, `displayCase('React') === 'React'` (already-cased input is left untouched).
    - `client/src/pages/Analysis.jsx` calls `displayCase(` at every keyword-badge render site (5 call sites: 3 in KeywordGroups, 2 in SectionFindings).
    - `client/src/pages/Analysis.jsx` no longer renders a bare `{kw}` or `{item}` as badge text without passing through `displayCase`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "import('./client/src/lib/keywordCasing.js').then(m=>{if(m.displayCase('sql')!=='SQL')throw new Error('sql casing failed');if(m.displayCase('React')!=='React')throw new Error('already-cased input was mutated');console.log('OK')})"</automated>
  </verify>
  <done>Analysis page keyword badges display SQL/API-style acronyms correctly-cased for heuristic output, while leaving any already-cased keyword string untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Job posting text (user-pasted) → extractKeywords() → match report / suggestion text | User-controlled free text is scanned against a fixed whitelist Set via substring/token matching, never used to construct a regex or executed as code |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-02-01 | Denial of Service | extractKeywords() multi-word phrase pass | low | accept | The new phrase-matching pass is a bounded `.includes()` scan over the ~20 multi-word `TECH_KEYWORDS` members against user text — no dynamic regex construction from user input (no ReDoS surface), and the whitelist size is small and static. |
| T-14-02-02 | Information Disclosure | Whitelist content (fairness) | low | mitigate | Only genuine, neutral product/data/business-skill vocabulary is added (see prohibitions); no coded or protected-characteristic-proxy terms are introduced. |

No high-severity findings. No new external dependencies, no new trust boundary crossed (same job-posting-text-in, keyword-list-out shape as before).
</threat_model>

<verification>
- `node server/lib/analysis/keywords.test.js` exits 0.
- `grep -c "ACRONYM_CASING" server/lib/analysis/keywords.js` ≥ 2 (definition + export).
- `grep -c "displayCase(" client/src/pages/Analysis.jsx` = 5 (all badge render sites updated).
</verification>

<success_criteria>
extractKeywords() correctly extracts every term in the CONTEXT.md example posting (not just react/sql). The Analysis page's keyword badges display known acronyms correctly-cased.
</success_criteria>

## Artifacts this phase produces

- `server/lib/analysis/keywords.test.js` — new test file
- `client/src/lib/keywordCasing.js` — new module, exports `ACRONYM_CASING`, `displayCase()`
- Modified: `server/lib/analysis/keywords.js` — new "Product / Data / Business skills" whitelist category, multi-word phrase matching in `extractKeywords()`, new `ACRONYM_CASING` export
- Modified: `client/src/pages/Analysis.jsx` — `KeywordGroups` and `SectionFindings` badge rendering routed through `displayCase()`

<output>
Create `.planning/phases/14-ux-quality-polish-from-user-feedback/14-02-SUMMARY.md` when done
</output>
