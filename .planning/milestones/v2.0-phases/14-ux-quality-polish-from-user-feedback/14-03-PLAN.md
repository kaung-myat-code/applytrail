---
phase: 14-ux-quality-polish-from-user-feedback
plan: 3
type: execute
wave: 2
depends_on: ["14-01", "14-02"]
files_modified:
  - server/lib/cover-letter.js
  - server/lib/cover-letter.test.js
  - server/lib/analysis/providers/heuristic.js
  - server/lib/analysis/providers/heuristic.test.js
  - server/package.json
autonomous: true
requirements: [UX-ISSUE-05]

must_haves:
  truths:
    - "generateCoverLetter() for a company name ending in 's' (e.g. 'Northstar Analytics') never produces a doubled-s possessive — output reads \"...Analytics' goals\", never \"...Analytics's goals\"."
    - "generateCoverLetter() for a company name NOT ending in 's' (e.g. 'Acme') still produces the standard possessive — output reads \"...Acme's goals\"."
    - "generateCoverLetter() output for two different (company, role) pairs with the same matched resume skills is not byte-identical — at minimum the intro or closing sentence varies (template variance, not pure keyword substitution)."
    - "generateSuggestions() summary/skills/experience/projects suggestion text displays known acronyms correctly-cased (e.g. 'Experienced in SQL', not 'Experienced in Sql') by routing every interpolated keyword through a displayCase() helper backed by keywords.js's ACRONYM_CASING."
    - "generateSuggestions() experience/project 'add' suggestions incorporate more than one missing keyword into a single bullet when two or more are available, instead of always producing one bullet per keyword from an identical template."
  artifacts:
    - path: "server/lib/cover-letter.test.js"
      provides: "Regression test proving the possessive-apostrophe fix and template variance"
      min_lines: 15
    - path: "server/lib/analysis/providers/heuristic.test.js"
      provides: "Regression test proving acronym-casing and genericness fixes in generateSuggestions()"
      min_lines: 15
  key_links:
    - from: "server/lib/analysis/providers/heuristic.js (displayCase)"
      to: "server/lib/analysis/keywords.js (ACRONYM_CASING)"
      via: "require('../keywords') destructuring ACRONYM_CASING alongside the existing extractKeywords/extractResumeKeywords import"
      pattern: "ACRONYM_CASING"
  prohibitions:
    - "MUST NOT introduce fabricated skills, achievements, or claims into generated cover-letter or suggestion text beyond what is present in the matched resume/job-posting content — template variance and multi-keyword incorporation must only rearrange and re-phrase existing matched strings, never invent new factual claims."
---

<objective>
Fix the confirmed possessive-apostrophe grammar bug and naive-capitalization casing bug in generated cover letters and suggestion text, and reduce genericness by varying sentence templates and incorporating more than one matched keyword per generated sentence/bullet. Closes GitHub #5.

Purpose: User-reported bugs (per D-14) — a company name ending in "s" should not get a doubled-s possessive, and generated text should display acronyms correctly-cased rather than naively capitalized — plus near-identical generated paragraphs across different job postings reduce trust in the generated content's quality. Test coverage in this plan stays targeted to the fixes made here, per the D-10 scope discretion.
Output: `server/lib/cover-letter.js` and `server/lib/analysis/providers/heuristic.js` both fixed and passing new regression tests; `server/package.json` test script extended to run all five server-side test files.
</objective>

<execution_context>
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/workflows/execute-plan.md
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-ux-quality-polish-from-user-feedback/14-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix possessive apostrophe + vary cover-letter templates</name>
  <files>server/lib/cover-letter.js, server/lib/cover-letter.test.js</files>
  <read_first>
    server/lib/cover-letter.js (full file — buildIntro at lines 96-105, buildClosing at lines 149-154, and the no-match fallback paragraph at lines 164-170, all of which currently hardcode `${company}'s`)
    server/lib/pdf.test.js (zero-dependency assert()+test() harness convention to replicate)
  </read_first>
  <behavior>
    - Test 1: a helper possessive('Northstar Analytics') === "Northstar Analytics'" (ends with s, lowercase-insensitive check).
    - Test 2: possessive('Acme') === "Acme's" (does not end with s).
    - Test 3: generateCoverLetter() called with two different job postings (different company/role, same resume) produces two paragraphs whose first sentence differs.
    - Test 4: no generated paragraph in any test case contains the literal substring "s's" immediately after a company name.
  </behavior>
  <action>
    Add a module-level `possessive(name)` function: if `name.toLowerCase().endsWith('s')`, return the name with a trailing apostrophe appended (no extra "s"); otherwise return the name with an apostrophe-s suffix appended. Replace the hardcoded company-possessive interpolation inside `buildClosing` (the "contribute to [company-possessive] goals" clause, which today unconditionally appends an apostrophe-s regardless of whether the company name already ends in "s") so it calls `possessive(company)` instead of interpolating the raw company name with a hardcoded suffix. Make the identical replacement in the no-match fallback paragraph inside `generateCoverLetter` (the "contribute to [company-possessive] goals" clause there has the same bug) so both call sites route through `possessive(company)`.
    Reduce genericness by adding two more phrasing variants each to `buildIntro` and `buildClosing`, selected deterministically (not randomly, so output stays reproducible/testable) via a small `pickVariant(variants, seed)` helper that sums the char codes of `seed` and takes `% variants.length`. `buildIntro(role, company, matchedSkills)`: keep the existing sentence as variant 0, add variant 1 ("I'm excited to apply for the ${role} role at ${company}, where my background${skillMention} directly supports what you're looking for.") and variant 2 ("With a background in software development${skillMention}, I believe I would be a strong fit for the ${role} position at ${company}."), selecting via `pickVariant([...], company + role)`. `buildClosing(company, role)`: keep the existing sentence as variant 0 (using `possessive(company)`), add variant 1 ("I would be glad to bring this experience to the ${role} role and help drive ${possessive(company)} goals forward.") and variant 2 ("I'm looking forward to the opportunity to support ${possessive(company)} goals as your next ${role}."), selecting via the same `pickVariant([...], company + role)` pattern so a given (company, role) pair is always deterministic but different pairs vary.
    Create server/lib/cover-letter.test.js following the pdf.test.js harness convention, covering all 4 behaviors above by calling the exported `generateCoverLetter` with two distinct fixture job postings against the same fixture resume.
  </action>
  <acceptance_criteria>
    - `server/lib/cover-letter.js` contains a `possessive` function definition.
    - No line in `server/lib/cover-letter.js` contains the literal template fragment `${company}'s` (both call sites now route through `possessive(company)`).
    - `node server/lib/cover-letter.test.js` exits 0 with no `FAIL:` lines.
  </acceptance_criteria>
  <verify>
    <automated>node server/lib/cover-letter.test.js</automated>
  </verify>
  <done>Possessive grammar is correct for company names ending in "s", and generated cover letters vary by (company, role) pair.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix acronym casing + reduce genericness in generateSuggestions(), consolidate npm test</name>
  <files>server/lib/analysis/providers/heuristic.js, server/lib/analysis/providers/heuristic.test.js, server/package.json</files>
  <read_first>
    server/lib/analysis/providers/heuristic.js (full file — `capitalize()` at lines 227-230, and every interpolation site of a raw lowercase `kw`/`missing`/`matched` entry inside `generateSuggestions` at lines 238-345: the summary modify/add block at 259-293, the skills-add loop at 296-311, the experience-add loop at 313-325, and the projects-add loop at 327-339)
    server/lib/analysis/keywords.js (the `ACRONYM_CASING` map added by Plan 14-02, Task 1 — this is the source of truth this task consumes)
    server/package.json (current test script after Plan 14-01: "node lib/pdf.test.js && node lib/defaultResumeData.test.js")
  </read_first>
  <behavior>
    - Test 1: generateSuggestions() for a resume with no summary and a report whose missing keywords include 'sql' produces a summary 'add' suggestion whose text contains "SQL" and does not contain "Sql".
    - Test 2: generateSuggestions() for a report with 2+ missing keywords produces at least one experience 'add' suggestion whose `suggested` text mentions two distinct keywords in one bullet (not one keyword per bullet only).
    - Test 3: two calls to generateSuggestions() with different missing-keyword sets produce experience 'add' bullets using different sentence templates (not always "Led {kw} initiatives...").
  </behavior>
  <action>
    Import `ACRONYM_CASING` alongside the existing `extractKeywords, extractResumeKeywords` destructure from `require('../keywords')` at the top of the file. Add a `displayCase(kw)` helper directly below the existing `capitalize()` function: return `ACRONYM_CASING[kw]` if present, otherwise fall back to `capitalize(kw)` (this is the generation-side helper — unlike Plan 14-02's client-side `displayCase`, all inputs here are always-lowercase extracted keywords, so an unconditional `capitalize()` fallback is correct and safe in this context).
    Replace every raw `capitalize(kw)` call and every raw lowercase `kw` string interpolation inside `generateSuggestions` with `displayCase(kw)`: the summary block's `topMissingForSummary.map(capitalize)` and `matched.slice(0, 3).map(capitalize)` (both become `.map(displayCase)`); the skills-add loop's `suggested: capitalize(kw)` and the reason string `"'" + kw + "' appears in the job posting..."` (both become `displayCase(kw)`).
    For the experience-add loop (`topMissingForExp`) and projects-add loop (`remainingForProjects`): change both from a strict one-keyword-per-iteration `for...of` loop to a paired loop that processes up to 2 keywords per generated suggestion (`for (let i = 0; i < list.length; i += 2) { const pair = list.slice(i, i + 2); ... }`), joining the pair's display-cased keywords with `' and '` for the sentence body and `' or '` for the reason clause. Add two more phrasing variants each (selected via the same deterministic `pickVariant(variants, seed)` pattern used in Plan 14-03 Task 1, keyed by the joined keyword text) so "Led {kw} initiatives that improved project delivery and team productivity." and "Implemented {kw} solutions to address real-world technical challenges." are no longer the only possible phrasings — add "Drove {kw} efforts, partnering across teams to ship measurable improvements." and "Applied {kw} to streamline workflows and boost team output." as experience variants, and "Built a project centered on {kw}, solving a concrete real-world problem." and "Delivered a {kw}-driven project from concept through completion." as project variants. Update both loops' `usedForExperience`/`usedForProjects` tracking to add every keyword in the pair, not just one.
    Create server/lib/analysis/providers/heuristic.test.js following the pdf.test.js harness convention, covering the 3 behaviors above using fixture resume/report objects shaped like `analyzeResume`'s real output.
    Finally, extend server/package.json's `"test"` script (currently `"node lib/pdf.test.js && node lib/defaultResumeData.test.js"` after Plan 14-01) to also run the two new test files from this phase: `"node lib/pdf.test.js && node lib/defaultResumeData.test.js && node lib/analysis/keywords.test.js && node lib/analysis/providers/heuristic.test.js && node lib/cover-letter.test.js"`.
  </action>
  <acceptance_criteria>
    - `server/lib/analysis/providers/heuristic.js` imports `ACRONYM_CASING` from `../keywords` and defines `displayCase`.
    - No remaining call to `capitalize(kw)` exists inside `generateSuggestions` (all replaced with `displayCase(kw)`); `capitalize` itself remains defined (still used inside `displayCase`'s fallback).
    - `node server/lib/analysis/providers/heuristic.test.js` exits 0 with no `FAIL:` lines.
    - `server/package.json`'s `test` script string contains all 5 test file paths: `lib/pdf.test.js`, `lib/defaultResumeData.test.js`, `lib/analysis/keywords.test.js`, `lib/analysis/providers/heuristic.test.js`, `lib/cover-letter.test.js`.
    - `cd server && npm test` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>cd server && npm test</automated>
  </verify>
  <done>Suggestion text displays acronyms correctly-cased, experience/project add-suggestions vary their template and can incorporate two keywords per bullet, and npm test runs all five server-side test files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Job posting text + resume content (both user-owned/pasted) → generateCoverLetter() / generateSuggestions() → text rendered verbatim in React (auto-escaped, no dangerouslySetInnerHTML) | Generated text is always assembled from fixed template strings plus substrings already present in the user's own resume/posting — never third-party or externally-fetched content |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-03-01 | Tampering | Generated text content accuracy | medium | mitigate | `possessive()`, `displayCase()`, and the template-variant helpers only rearrange/re-phrase/re-case strings already present in `matchedSkills`, `matchedExperience`, `matchedProjects`, or the extracted keyword lists — no new factual claims are ever introduced (see prohibitions). |
| T-14-03-02 | Information Disclosure | Cover letter / suggestion text rendering | low | accept | Output is rendered via React JSX text interpolation (`{coverLetter}`, `{suggestion.suggested}`), which auto-escapes; no `dangerouslySetInnerHTML` is used anywhere in the consuming components, unchanged by this plan. |

No high-severity findings. No new external dependencies or trust boundaries.
</threat_model>

<verification>
- `node server/lib/cover-letter.test.js` exits 0.
- `node server/lib/analysis/providers/heuristic.test.js` exits 0.
- `cd server && npm test` exits 0 (all 5 test files pass).
- `grep -c "\${company}'s" server/lib/cover-letter.js` = 0.
</verification>

<success_criteria>
Cover letters never produce a doubled-s possessive for company names ending in "s", suggestion text displays known acronyms correctly-cased, and both generated-text surfaces vary their phrasing across different inputs instead of reading identically templated.
</success_criteria>

## Artifacts this phase produces

- `server/lib/cover-letter.test.js` — new test file
- `server/lib/analysis/providers/heuristic.test.js` — new test file
- Modified: `server/lib/cover-letter.js` — new `possessive()` and `pickVariant()` helpers, `buildIntro`/`buildClosing` template-variant arrays
- Modified: `server/lib/analysis/providers/heuristic.js` — new `displayCase()` helper, `EXPERIENCE_TEMPLATES`/`PROJECT_TEMPLATES` variant arrays, paired-keyword suggestion loops
- Modified: `server/package.json` — `test` script finalized to run all 5 server-side test files

<output>
Create `.planning/phases/14-ux-quality-polish-from-user-feedback/14-03-SUMMARY.md` when done
</output>
