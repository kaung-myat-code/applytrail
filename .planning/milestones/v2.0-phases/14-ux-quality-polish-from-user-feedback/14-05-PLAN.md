---
phase: 14-ux-quality-polish-from-user-feedback
plan: 5
type: execute
wave: 2
depends_on: ["14-02"]
files_modified:
  - client/src/components/Navbar/Navbar.jsx
  - client/src/components/Navbar/Navbar.module.css
  - client/src/components/Navbar/Navbar.test.jsx
  - client/src/pages/Analysis.jsx
autonomous: true
requirements: [UX-ISSUE-07]

must_haves:
  truths:
    - "Top-level navigation is collapsed into grouped sections reflecting the linear job-search workflow: standalone Dashboard, New Application, and Applications links, plus a 'Resume' group (Resume, Resume Library) and a 'Tailor' group (Analysis, Cover Letter), per D-05."
    - "Exactly one nav dropdown group is open at a time — opening the 'Tailor' group while 'Resume' is open closes 'Resume' automatically."
    - "The open nav dropdown closes when the user presses Escape, clicks outside the panel, or selects a child link."
    - "A nav group's label renders with the active/accent treatment whenever the current route matches any of its children, mirroring the existing .link.active treatment."
    - "'New Application' and 'Applications' remain standalone top-level NavLinks, not nested inside any dropdown group."
    - "Each dropdown panel sizes to its exactly-2-children content with no internal scrollbar — the existing --space-2/--space-3 padding pattern already used for standalone links scales without layout change if a group ever grows a third child."
    - "The Analysis page's Review Suggestions link reads 'Continue to Review Suggestions →' (per D-06), using the same destination and state payload as before — only the copy changes."
    - statement: "The nav dropdown panel for the rightmost group ('Tailor') does not visually clip off the right edge of narrow viewports, because the panel is anchored with right:0 rather than left:0."
      verification: backstop
  artifacts:
    - path: "client/src/components/Navbar/Navbar.test.jsx"
      provides: "Regression test proving one-group-open-at-a-time, Escape-to-close, and outside-click-to-close behavior for the new nav dropdown"
      min_lines: 15
  key_links:
    - from: "client/src/components/Navbar/Navbar.jsx (NavGroup)"
      to: "react-router-dom useLocation"
      via: "useLocation().pathname compared against each child's `to` value to compute the group's active state"
      pattern: "useLocation"
  prohibitions:
    - "MUST NOT bury 'New Application' or 'Applications' inside a collapsed dropdown group — these remain single-click-accessible standalone top-level items, since they are the most frequently used entry points in the linear workflow this restructuring is meant to reinforce."
---

<objective>
Collapse the 7 flat top-level nav items into grouped sections reflecting the linear job-search workflow (Dashboard, Resume group, New Application, Tailor group, Applications), and add a "Continue to Review Suggestions →" accent CTA on the Analysis page. Closes GitHub #7.

Purpose: The current 7-item flat nav bar does not communicate the linear order of the workflow (resume → analyze → tailor → apply → track); grouping related pages and adding contextual continue-CTAs reinforces the intended path.
Output: `Navbar.jsx` renders a mixed array of standalone links and click-to-open dropdown groups; `Analysis.jsx`'s existing Review Suggestions link is relabeled with the continue-CTA copy.
</objective>

<execution_context>
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/workflows/execute-plan.md
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-ux-quality-polish-from-user-feedback/14-CONTEXT.md
@.planning/phases/14-ux-quality-polish-from-user-feedback/14-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Grouped nav dropdown structure and interaction</name>
  <files>client/src/components/Navbar/Navbar.jsx, client/src/components/Navbar/Navbar.module.css, client/src/components/Navbar/Navbar.test.jsx</files>
  <read_first>
    client/src/components/Navbar/Navbar.jsx (full file — the flat `navLinks` array at lines 4-12 and the render loop at lines 20-32, both replaced by this task)
    client/src/components/Navbar/Navbar.module.css (full file — `.link`/`.link.active` rules at lines 42-62, reused unmodified for both standalone items and dropdown children)
    client/src/components/CreateApplicationModal.jsx (lines 61-70 — the Escape-key `useEffect` idiom to replicate for closing an open dropdown; lines 108-113 — the backdrop-click-to-close idiom to adapt for outside-click detection via a `ref` and a `mousedown` document listener)
    client/src/components/CreateApplicationModal.test.jsx (the vitest + @testing-library/react render/fireEvent/waitFor pattern already established in this repo for interaction testing)
  </read_first>
  <behavior>
    - Test 1: clicking the "Resume" group label opens a panel containing "Resume" and "Resume Library" links.
    - Test 2: with "Resume" open, clicking the "Tailor" group label closes "Resume"'s panel and opens "Tailor"'s panel (only one open at a time).
    - Test 3: with a group open, pressing Escape closes it.
    - Test 4: with a group open, a click outside the navbar closes it.
    - Test 5: "New Application" and "Applications" render as plain links (no dropdown trigger) at every point in the test.
  </behavior>
  <action>
    Replace the flat `navLinks` array in `client/src/components/Navbar/Navbar.jsx` with a `navItems` array of two shapes: `{ type: 'link', to, label }` for standalone items, and `{ type: 'group', label, children: [{ to, label }, ...] }` for dropdown groups. Populate it as: Dashboard (link, `/`), Resume (group: Resume `/resume`, Resume Library `/resume-library`), New Application (link, `/new`), Tailor (group: Analysis `/analysis`, Cover Letter `/cover-letter`), Applications (link, `/applications`) — this exact grouping and order matches D-05/D-07 and the UI-SPEC's proposed top-level order.
    Add `const [openGroup, setOpenGroup] = useState(null)` (tracks which group label is currently open, or `null`). Add a `navRef = useRef(null)` on the outermost `<nav>` element. Add a `useEffect` that attaches a `document.addEventListener('mousedown', handleClickOutside)` when `openGroup` is not null, where `handleClickOutside` calls `setOpenGroup(null)` if `navRef.current` exists and `!navRef.current.contains(event.target)`; clean up the listener when the effect re-runs or unmounts. Add a second `useEffect` (also gated on `openGroup !== null`) that closes on Escape via `document.addEventListener('keydown', ...)`, mirroring `CreateApplicationModal.jsx`'s existing Escape idiom.
    Render standalone items exactly as today (`NavLink` with the existing `styles.link`/`styles.active` className function). Render each group as a `<div>` containing: a `<button type="button">` trigger showing the group label plus a trailing `▾` glyph, `onClick` toggling `openGroup` between this group's label and `null`; and — when `openGroup === thisGroup.label` — a dropdown panel `<div>` (new `styles.dropdownPanel` class) containing a `NavLink` per child (reusing `styles.link`/`styles.active`), each with `onClick={() => setOpenGroup(null)}` so selecting a child closes the panel. Compute the trigger's own active styling via `useLocation().pathname` (import `useLocation` alongside the existing `NavLink` import from `react-router-dom`) — the trigger gets `styles.active` applied whenever `pathname` matches any child's `to`.
    Add `.dropdownPanel` to `Navbar.module.css`: `position: absolute`, `top: 100%`, `background: var(--color-surface)`, `border: 1px solid var(--color-border-light)`, `border-radius: var(--radius-md)`, `box-shadow: var(--shadow-md)`, `padding: var(--space-2)`, `display: flex`, `flex-direction: column`, `gap: var(--space-1)`, `z-index: 10`. Add a `.groupTrigger` class reusing the same padding/font/color rules as `.link` (copy the declarations, since a `<button>` needs its own reset: `background: none`, `border: none`, `font-family: inherit`, `cursor: pointer`) plus `.groupTrigger.active` mirroring `.link.active`. On the LAST group ("Tailor"), anchor its dropdown panel with `right: 0` instead of the default `left: 0` so it never clips off the right edge of narrow viewports (the backstop truth in this plan's must_haves) — either via a dedicated `.dropdownPanelRight` modifier class applied only to the Tailor group, or an inline style; prefer the modifier class for consistency with the rest of the file's CSS Modules convention. Add a mobile (`max-width: 640px`) rule making `.dropdownPanel` render `position: static` (inline/expanded, not floating), consistent with the existing mobile stacking behavior in this file.
    Create `client/src/components/Navbar/Navbar.test.jsx` using `vitest` + `@testing-library/react` (wrap `Navbar` in a `MemoryRouter` from `react-router-dom`, following the render/fireEvent/waitFor conventions already used in `CreateApplicationModal.test.jsx`) covering the 5 behaviors above.
  </action>
  <acceptance_criteria>
    - `client/src/components/Navbar/Navbar.jsx` defines a `navItems` array containing both `type: 'link'` and `type: 'group'` entries, with "Resume" and "Tailor" as the only two groups.
    - Clicking one group's trigger while another is open closes the first (only one `openGroup` value at a time).
    - An Escape keydown and a click outside `navRef.current` both close an open group.
    - "New Application" and "Applications" render as top-level `NavLink`s with no dropdown affordance.
    - `client/src/components/Navbar/Navbar.module.css` defines `.dropdownPanel`, `.groupTrigger`, and a right-anchored modifier for the last group's panel.
    - `cd client && npx vitest run src/components/Navbar/Navbar.test.jsx` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd client && npx vitest run src/components/Navbar/Navbar.test.jsx</automated>
  </verify>
  <done>Navigation is grouped per D-05, one dropdown open at a time, closes on Escape/outside-click/selection, and the two highest-traffic standalone entry points remain single-click accessible.</done>
</task>

<task type="auto">
  <name>Task 2: Continue-to-Review-Suggestions CTA on Analysis page</name>
  <files>client/src/pages/Analysis.jsx</files>
  <read_first>
    client/src/pages/Analysis.jsx (the review-suggestions Link block at lines 350-360 — this task only changes its visible text)
    client/src/pages/Analysis.module.css (`.reviewButton` rule, lines 441-457 — already uses `var(--color-primary)`, confirming no CSS change is needed for accent treatment)
  </read_first>
  <action>
    In the `Link` inside the `.reviewLink` block (the one navigating to `/analysis/review?...`), change the visible text from `Review {suggestions.length} Suggestions` to `Continue to Review Suggestions →` (per the UI-SPEC Copywriting Contract — the suggestion count is no longer shown in the label itself). Do not change the `to`, `state`, or `className` props on this `Link` — only the text node changes.
  </action>
  <acceptance_criteria>
    - The Review Suggestions `Link` in `client/src/pages/Analysis.jsx` renders the text "Continue to Review Suggestions →".
    - The `Link`'s `to`, `state`, and `className` props are byte-identical to before this task.
  </acceptance_criteria>
  <verify>
    <automated>cd client && grep -q "Continue to Review Suggestions" src/pages/Analysis.jsx && echo "OK"</automated>
  </verify>
  <done>The Analysis page's path to Review Suggestions reads as an explicit "continue" step in the linear workflow.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Purely client-side navigation UI; no new data crosses any trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-05-01 | Denial of Service | Outside-click/Escape listeners | low | accept | Listeners are only attached while `openGroup !== null` and are removed on cleanup — no listener leak across renders, consistent with the existing `CreateApplicationModal` pattern this mirrors. |

No high-severity findings. No new external dependencies, no server-side surface touched.
</threat_model>

<verification>
- `cd client && npx vitest run src/components/Navbar/Navbar.test.jsx` passes.
- `grep -q "Continue to Review Suggestions" client/src/pages/Analysis.jsx` succeeds.
- `cd client && npm run build` succeeds.
</verification>

<success_criteria>
Navigation visually communicates the linear workflow through grouping, one dropdown open at a time, with the two highest-traffic pages always one click away.
</success_criteria>

## Artifacts this phase produces

- `client/src/components/Navbar/Navbar.test.jsx` — new test file
- New CSS classes: `.dropdownPanel`, `.groupTrigger`, `.groupTrigger.active`, right-anchored panel modifier in `client/src/components/Navbar/Navbar.module.css`
- Modified: `client/src/components/Navbar/Navbar.jsx` — `navItems` array (mixed link/group shapes), `openGroup` state, outside-click/Escape close handlers
- Modified: `client/src/pages/Analysis.jsx` — Review Suggestions link relabeled to "Continue to Review Suggestions →"

<output>
Create `.planning/phases/14-ux-quality-polish-from-user-feedback/14-05-SUMMARY.md` when done
</output>
