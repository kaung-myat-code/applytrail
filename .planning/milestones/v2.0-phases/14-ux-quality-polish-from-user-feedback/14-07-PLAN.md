---
phase: 14-ux-quality-polish-from-user-feedback
plan: 7
type: execute
wave: 1
depends_on: []
files_modified:
  - client/eslint.config.js
  - client/package.json
  - client/package-lock.json
autonomous: false

requirements: [UX-ISSUE-08]

must_haves:
  truths:
    - "client/dist (the built React bundle) is excluded from ESLint via a global ignores entry in client/eslint.config.js, so the ~323 minified-bundle errors currently reported against it disappear."
    - "The prop-types npm package is added as an explicit direct devDependency of client/package.json (not left as an implicit transitive resolution), pinned to the version already present in node_modules."
    - "Running npx eslint . from client/ after this plan reports zero errors against anything under client/dist/."
  artifacts: []
  key_links: []
  prohibitions:
    - "MUST NOT achieve a clean `eslint .` pass by disabling or weakening ESLint rules (e.g. blanket eslint-disable comments, removing react/prop-types from the ruleset) — the sole rule-set change in this phase is the client/dist ignore entry; every other currently-reported error must be fixed at the source in Plan 14-08, never suppressed."
---

<objective>
Prerequisite setup for the ESLint cleanup mandated by D-12 (GitHub #8): exclude the built `client/dist` bundle from linting, and add `prop-types` as a real direct dependency so Plan 14-08 can add PropTypes definitions to the flagged components. Part of closing GitHub #8 — Plan 14-08 completes it.

Purpose: `npx eslint .` currently reports 411 problems, of which 323 are from the minified production bundle at `client/dist/assets/index-*.js` that should never have been linted in the first place. The remaining 88 real errors need the `prop-types` package to fix, which is currently only a phantom transitive dependency, not declared.
Output: `client/eslint.config.js` ignores `dist`; `client/package.json` declares `prop-types` as a direct devDependency.
</objective>

<execution_context>
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/workflows/execute-plan.md
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-ux-quality-polish-from-user-feedback/14-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy checkpoint for prop-types</name>
  <what-built>
    No source code has been written yet — this checkpoint gates the next task, which adds `prop-types` as a new direct npm dependency of `client/package.json`. No `RESEARCH.md` exists for this phase (research was explicitly skipped), so there is no Package Legitimacy Audit table on file. Per the package-legitimacy-gate fallback policy, `prop-types` is treated as `[ASSUMED]` and requires human sign-off on npmjs.com before install, even though it is Facebook/Meta's own long-standing React ecosystem package and is already present transitively in this exact project's `client/package-lock.json` (v15.8.1, via a dependency of `react-diff-viewer-continued`).
  </what-built>
  <how-to-verify>
    Visit https://www.npmjs.com/package/prop-types and confirm: (1) the package name is exactly `prop-types` (no typosquat variants like `prop-type` or `proptypes`), (2) the maintainer/publisher is Facebook/Meta's React org (`facebook` npm org or the current React maintainers), (3) it has a long publish history with millions of weekly downloads, consistent with `client/package-lock.json`'s existing transitive entry at version `15.8.1`.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with adding prop-types as a direct dependency, or describe concerns to halt.</resume-signal>
  <action>Verify the prop-types package identity/publisher/download-history on npmjs.com per the how-to-verify steps above before Task 2 installs it as a direct dependency.</action>
  <verify>
    <human-check>Package identity, publisher, and download history confirmed legitimate on npmjs.com</human-check>
  </verify>
  <done>Human has confirmed prop-types is the genuine, legitimate npm package before it is added as a direct dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Exclude client/dist from ESLint and add prop-types as a direct dependency</name>
  <files>client/eslint.config.js, client/package.json, client/package-lock.json</files>
  <read_first>
    client/eslint.config.js (full file — ESLint 9 flat-config format, currently has no `ignores` entry at all)
    client/package.json (full file — `devDependencies` block, where `prop-types` will be added alongside the existing `@eslint/js`/`eslint`/`eslint-plugin-react` entries)
  </read_first>
  <action>
    In `client/eslint.config.js`, add a new object as the FIRST element of the exported array: `{ ignores: ['dist/**'] }` — this is ESLint 9's flat-config global-ignore convention (an object whose ONLY key is `ignores`, applied before any other config in the array is evaluated). Do not modify any other rule, plugin, or config object in this file.
    In `client/package.json`, add `"prop-types": "^15.8.1"` to the `devDependencies` object (pin to the version already resolved in `node_modules`/`package-lock.json` from Task 1's checkpoint, so no new network fetch is required for the version already present). Run `npm install --prefix client` (or `cd client && npm install`) so `client/package-lock.json` records `prop-types` as a direct dependency instead of only a transitive one.
  </action>
  <acceptance_criteria>
    - `client/eslint.config.js`'s exported array's first element is `{ ignores: ['dist/**'] }` (or an equivalent glob covering the whole `dist` directory).
    - `client/package.json`'s `devDependencies` contains a `prop-types` entry.
    - `require.resolve('prop-types')` from within `client/` resolves successfully after `npm install`.
    - `cd client && npx eslint . 2>&1 | grep -c "client/dist"` returns `0` (no errors reported against the dist bundle).
  </acceptance_criteria>
  <verify>
    <automated>cd client && npx eslint . 2>&1 | grep -c "dist/assets" | grep -qx 0 && echo "OK"</automated>
  </verify>
  <done>ESLint no longer scans the built bundle, and prop-types is a real, installed, directly-declared dependency ready for Plan 14-08 to import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → client/package.json / node_modules | A new direct dependency (`prop-types`) is added; the package itself is untrusted third-party code that runs at build/dev time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-07-SC | Tampering | `npm install` of `prop-types` | high | mitigate | Blocking human checkpoint (Task 1) requires npmjs.com verification of package identity/publisher/download history before install; version pinned to the exact one already present transitively in `client/package-lock.json`, so no unreviewed new code enters the dependency tree. |

No other findings. `prop-types` is a zero-runtime-behavior type-checking utility (dev-time warnings only), not wired into any request-handling or data-persistence path.
</threat_model>

<verification>
- `cd client && npx eslint . 2>&1 | tail -5` shows a problem count of 88 or fewer (down from 411), with zero remaining references to `client/dist`.
- `cd client && node -e "require.resolve('prop-types')"` exits 0.
</verification>

<success_criteria>
`npx eslint .` no longer scans the built bundle, and `prop-types` is available as a real, directly-declared dependency for Plan 14-08.
</success_criteria>

## Artifacts this phase produces

- Modified: `client/eslint.config.js` — new `{ ignores: ['dist/**'] }` global-ignore config object
- Modified: `client/package.json` — `prop-types` added to `devDependencies`
- Modified: `client/package-lock.json` — `prop-types` recorded as a direct dependency

<output>
Create `.planning/phases/14-ux-quality-polish-from-user-feedback/14-07-SUMMARY.md` when done
</output>
