---
phase: 14-ux-quality-polish-from-user-feedback
plan: 8
type: execute
wave: 3
depends_on: ["14-02", "14-05", "14-07"]
files_modified:
  - client/src/components/CreateApplicationModal.jsx
  - client/src/components/ResumeDiffViewer.jsx
  - client/src/components/SectionEditor.jsx
  - client/src/components/SuggestionCard.jsx
  - client/src/pages/Analysis.jsx
  - client/src/pages/Dashboard.jsx
autonomous: true
requirements: [UX-ISSUE-08]

must_haves:
  truths:
    - "Running npx eslint . from client/ exits 0 with zero errors and zero warnings across the entire client/ tree (dist excluded per Plan 14-07)."
    - "Every component flagged by react/prop-types (CreateApplicationModal, ResumeDiffViewer, SectionEditor, SuggestionCard, and Analysis's internal ScoreDisplay/KeywordGroups/SectionFindings) declares a real PropTypes.propTypes object matching its actual props, imported from the prop-types package added in Plan 14-07 — not a rule suppression."
    - "The unused useNavigate import and navigate variable in Analysis.jsx are removed entirely (they were dead code, not wired to any click handler)."
    - "Dashboard.jsx's unescaped apostrophe in 'job listings you're interested in' is fixed via HTML entity escaping, not by rewording the sentence."
  artifacts: []
  key_links: []
  prohibitions:
    - "MUST NOT achieve a clean eslint pass by disabling or weakening rules (e.g. blanket eslint-disable comments, loosening react/prop-types or no-unused-vars in client/eslint.config.js) — every error fixed in this plan must be a real PropTypes definition, a real removal of dead code, or a real entity-escape; client/eslint.config.js itself is not modified by this plan (only Plan 14-07 touches it, for the dist ignore)."
---

<objective>
Fix every remaining real ESLint error in the client/ source tree, per D-12 — missing PropTypes on 5 components and their 2 unused-var/unescaped-entity errors — bringing `npx eslint .` to a clean, enforceable zero-error baseline. Completes closing GitHub #8 (Plan 14-07 handled the prerequisite dist-exclusion and prop-types install).

Purpose: A clean lint baseline is what makes ESLint useful as a regression gate going forward; 88 real (non-bundle) errors currently make `npx eslint .` useless as a signal.
Output: `CreateApplicationModal.jsx`, `ResumeDiffViewer.jsx`, `SectionEditor.jsx`, `SuggestionCard.jsx`, `Analysis.jsx`, `Dashboard.jsx` all pass `react/prop-types`, `no-unused-vars`, and `react/no-unescaped-entities` with zero suppressions.
</objective>

<execution_context>
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/workflows/execute-plan.md
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/14-ux-quality-polish-from-user-feedback/14-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PropTypes for CreateApplicationModal, ResumeDiffViewer, SectionEditor</name>
  <files>client/src/components/CreateApplicationModal.jsx, client/src/components/ResumeDiffViewer.jsx, client/src/components/SectionEditor.jsx</files>
  <read_first>
    client/src/components/CreateApplicationModal.jsx (function signature at lines 6-16 — 8 destructured props: mode, company, role, postingText, postingId, resumeVersionId, resumeVersionName, onCancel, onSuccess)
    client/src/components/ResumeDiffViewer.jsx (function signature at line 4 — 2 destructured props: current, suggested)
    client/src/components/SectionEditor.jsx (function signature at line 3 — 2 destructured props: title, children)
  </read_first>
  <action>
    In each of the 3 files, add `import PropTypes from 'prop-types'` at the top (after the existing React/router imports, before the local styles import), and add a `ComponentName.propTypes = { ... }` assignment immediately after the component function definition (before its `export default` line, matching the position convention used throughout this codebase for other trailing statements).
    `CreateApplicationModal.propTypes`: `mode: PropTypes.string`, `company: PropTypes.string`, `role: PropTypes.string`, `postingText: PropTypes.string`, `postingId: PropTypes.string`, `resumeVersionId: PropTypes.string`, `resumeVersionName: PropTypes.string`, `onCancel: PropTypes.func.isRequired`, `onSuccess: PropTypes.func.isRequired`.
    `ResumeDiffViewer.propTypes`: `current: PropTypes.string`, `suggested: PropTypes.string`.
    `SectionEditor.propTypes`: `title: PropTypes.string.isRequired`, `children: PropTypes.node`.
  </action>
  <acceptance_criteria>
    - All 3 files import `PropTypes` from `'prop-types'`.
    - Each component has a `.propTypes` assignment whose keys exactly match its destructured prop names.
    - `cd client && npx eslint src/components/CreateApplicationModal.jsx src/components/ResumeDiffViewer.jsx src/components/SectionEditor.jsx` reports zero errors.
  </acceptance_criteria>
  <verify>
    <automated>cd client && npx eslint src/components/CreateApplicationModal.jsx src/components/ResumeDiffViewer.jsx src/components/SectionEditor.jsx</automated>
  </verify>
  <done>Three components have complete, accurate PropTypes declarations and produce zero ESLint errors.</done>
</task>

<task type="auto">
  <name>Task 2: PropTypes for SuggestionCard and Analysis's internal components, plus unused-var and unescaped-entity fixes</name>
  <files>client/src/components/SuggestionCard.jsx, client/src/pages/Analysis.jsx, client/src/pages/Dashboard.jsx</files>
  <read_first>
    client/src/components/SuggestionCard.jsx (function signature at line 12 — 5 props: suggestion, decision, onAccept, onReject, onEdit; note `suggestion.type` is one of 'add'/'modify'/'remove' per the `typeLabels`/`typeColors` dicts at lines 5-10, and `decision.status` is one of 'accepted'/'rejected'/'edited')
    client/src/pages/Analysis.jsx (three internal function components: ScoreDisplay lines 5-41, KeywordGroups lines 43-87, SectionFindings lines 89-150; the unused `navigate`/`useNavigate` at lines 1-2 and 153 — confirm `navigate` has zero other call sites in the file before removing)
    client/src/pages/Dashboard.jsx (line 168 — the unescaped apostrophe in "job listings you're interested in" inside the Getting Started steps list)
  </read_first>
  <action>
    In `SuggestionCard.jsx`, add `import PropTypes from 'prop-types'` and, after the component definition, `SuggestionCard.propTypes = { suggestion: PropTypes.shape({ id: PropTypes.string, type: PropTypes.oneOf(['add', 'modify', 'remove']), section: PropTypes.string, current: PropTypes.string, suggested: PropTypes.string, reason: PropTypes.string }).isRequired, decision: PropTypes.shape({ status: PropTypes.oneOf(['accepted', 'rejected', 'edited']), editedContent: PropTypes.string }), onAccept: PropTypes.func.isRequired, onReject: PropTypes.func.isRequired, onEdit: PropTypes.func.isRequired }`.
    In `Analysis.jsx`, add `import PropTypes from 'prop-types'` near the top. After `ScoreDisplay`'s definition, add `ScoreDisplay.propTypes = { score: PropTypes.number.isRequired, summary: PropTypes.string, strengths: PropTypes.arrayOf(PropTypes.string), gaps: PropTypes.arrayOf(PropTypes.string) }`. After `KeywordGroups`'s definition, add `KeywordGroups.propTypes = { keywords: PropTypes.shape({ matched: PropTypes.arrayOf(PropTypes.string), missing: PropTypes.arrayOf(PropTypes.string), bonus: PropTypes.arrayOf(PropTypes.string) }).isRequired }`. After `SectionFindings`'s definition, add `SectionFindings.propTypes = { sections: PropTypes.objectOf(PropTypes.shape({ matchRate: PropTypes.number, summary: PropTypes.string, matchedItems: PropTypes.arrayOf(PropTypes.string), missingItems: PropTypes.arrayOf(PropTypes.string) })).isRequired }`. Remove `useNavigate` from the `react-router-dom` import (keep `Link`) and delete the `const navigate = useNavigate()` line inside the `Analysis` component — this variable has no call sites anywhere in the file (confirmed dead code from an earlier iteration).
    In `Dashboard.jsx`, replace the literal apostrophe in "job listings you're interested in" with the HTML entity `&apos;` (matching the exact escaping convention already used elsewhere in this codebase, e.g. `CreateApplicationModal.jsx`'s "Couldn&apos;t auto-generate...") so the sentence renders identically but satisfies `react/no-unescaped-entities`.
  </action>
  <acceptance_criteria>
    - `SuggestionCard.jsx` and `Analysis.jsx` both import `PropTypes` and declare complete `.propTypes` objects for every flagged component.
    - `Analysis.jsx` no longer imports `useNavigate` and no longer declares a `navigate` variable.
    - `Dashboard.jsx` uses `&apos;` in place of the literal apostrophe in the "you're interested in" sentence.
    - `cd client && npx eslint .` exits 0 with zero errors and zero warnings.
  </acceptance_criteria>
  <verify>
    <automated>cd client && npx eslint .</automated>
  </verify>
  <done>npx eslint . passes cleanly across the entire client/ tree with no suppressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Static analysis / dev-time type-checking only; no runtime behavior change, no new trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-08-01 | Tampering | Lint-gaming via rule suppression | low | mitigate | Every fix in this plan is a real PropTypes declaration, a real dead-code removal, or a real entity-escape — no `eslint-disable` comment or rule-weakening is introduced anywhere in this plan (see prohibitions); `client/eslint.config.js` itself is untouched by this plan. |

No high-severity findings. Purely dev-tooling/static-analysis scope.
</threat_model>

<verification>
- `cd client && npx eslint .` exits 0.
- `cd client && npm run build` succeeds.
- `cd client && npx vitest run` still passes (no test regressions from the PropTypes additions or the Analysis.jsx dead-code removal).
</verification>

<success_criteria>
`npx eslint .` is a clean, trustworthy, zero-error/zero-warning gate across the entire client/ tree.
</success_criteria>

## Artifacts this phase produces

- Modified: `client/src/components/CreateApplicationModal.jsx` — `.propTypes` added
- Modified: `client/src/components/ResumeDiffViewer.jsx` — `.propTypes` added
- Modified: `client/src/components/SectionEditor.jsx` — `.propTypes` added
- Modified: `client/src/components/SuggestionCard.jsx` — `.propTypes` added
- Modified: `client/src/pages/Analysis.jsx` — `.propTypes` added to `ScoreDisplay`/`KeywordGroups`/`SectionFindings`; unused `useNavigate`/`navigate` removed
- Modified: `client/src/pages/Dashboard.jsx` — unescaped apostrophe fixed to `&apos;`

<output>
Create `.planning/phases/14-ux-quality-polish-from-user-feedback/14-08-SUMMARY.md` when done
</output>
