---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - client/package.json
  - client/vite.config.js
  - client/eslint.config.js
  - client/.prettierrc
  - client/index.html
  - client/src/main.jsx
  - client/src/App.jsx
  - client/src/App.module.css
  - client/src/index.css
  - client/src/setupTests.js
  - client/src/components/Navbar/Navbar.jsx
  - client/src/components/Navbar/Navbar.module.css
  - client/src/pages/Dashboard.jsx
  - client/src/pages/Resume.jsx
  - client/src/pages/NewApplication.jsx
  - client/src/pages/Applications.jsx
  - server/package.json
  - server/index.js
  - resume.json
autonomous: true
requirements:
  - RESUME-02
user_setup: []

must_haves:
  truths:
    - "App starts with `npm run dev` and loads in the browser at localhost:5173"
    - "User can click nav links to navigate between Dashboard, Resume, New Application, and Applications pages"
    - "GET /api/applications returns existing applications.json data"
    - "GET /api/resume returns resume.json data"
    - "Data persists across server restarts (JSON files are read from disk, not memory)"
  artifacts:
    - "package.json (root with concurrently scripts)"
    - "client/package.json (React + Vite + ESLint + Vitest)"
    - "client/vite.config.js (proxy to Express)"
    - "server/package.json (Express)"
    - "server/index.js (Express server with API routes)"
    - "resume.json (migrated from resume.md)"
    - "client/src/main.jsx (React Router with createBrowserRouter)"
    - "client/src/App.jsx (layout with Outlet)"
    - "client/src/components/Navbar/Navbar.jsx (navigation links)"
  key_links:
    - "Vite proxy forwards /api/* to Express on port 3000"
    - "React Router Outlet renders child pages inside App layout"
    - "Express readJSON reads from project root (DATA_DIR = path.join(__dirname, '..'))"
    - "Navbar NavLink components use react-router-dom for client-side navigation"
---

<objective>
Scaffold a working React + Express monorepo with JSON file persistence and a navigable app shell.

Purpose: Establish the foundation that all subsequent phases build on — the user can start the app, navigate between pages, and verify that API routes serve persisted JSON data.

Output: A runnable full-stack app with root concurrently scripts, Vite dev server with proxy, Express API serving applications.json and resume.json, React Router with four placeholder pages, and a top navigation bar.
</objective>

<execution_context>
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/workflows/execute-plan.md
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@applications.json
@resume.md
@.planning/phases/01-foundation/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: App Shell — Scaffold monorepo, navigation, and placeholder pages</name>
  <files>
    package.json,
    client/package.json,
    client/vite.config.js,
    client/eslint.config.js,
    client/.prettierrc,
    client/index.html,
    client/src/main.jsx,
    client/src/App.jsx,
    client/src/App.module.css,
    client/src/index.css,
    client/src/setupTests.js,
    client/src/components/Navbar/Navbar.jsx,
    client/src/components/Navbar/Navbar.module.css,
    client/src/pages/Dashboard.jsx,
    client/src/pages/Resume.jsx,
    client/src/pages/NewApplication.jsx,
    client/src/pages/Applications.jsx
  </files>
  <action>
    Create the full monorepo scaffolding so the user can start the app and navigate between pages.

    **Root package.json:**
    - Name: "applytrail", private: true
    - Scripts: "dev" uses concurrently to run "dev:client" and "dev:server" with labels client,server and colors blue,green
    - "dev:client": cd client && npm run dev
    - "dev:server": cd server && node index.js
    - "lint": cd client && npm run lint
    - "test": cd client && npm run test
    - devDependencies: concurrently ^9.0.0

    **client/package.json:**
    - Name: "applytrail-client", private: true
    - Type: "module"
    - Scripts: "dev" (vite), "build" (vite build), "lint" (eslint .), "test" (vitest)
    - Dependencies: react, react-dom, react-router-dom
    - DevDependencies: @vitejs/plugin-react, vite, eslint, @eslint/js, globals, eslint-plugin-react, eslint-plugin-react-hooks, eslint-config-prettier, prettier, vitest, @testing-library/react, @testing-library/jest-dom, jsdom

    **client/vite.config.js:**
    - Import defineConfig from vite and react from @vitejs/plugin-react
    - Add triple-slash reference for vitest types: /// <reference types="vitest" />
    - Plugins: [react()]
    - Server port: 5173
    - Proxy: '/api' target 'http://localhost:3000', changeOrigin: true
    - Test config: globals true, environment 'jsdom', setupFiles './src/setupTests.js'

    **client/eslint.config.js:**
    - ESLint 9 flat config format (export default array)
    - Extend js.configs.recommended
    - Add react and react-hooks plugins for **/*.{js,jsx} files
    - globals.browser for languageOptions
    - Parser options with ecmaFeatures.jsx: true
    - Rules: react-hooks/rules-of-hooks error, react-hooks/exhaustive-deps warn
    - Append prettierConfig as last element

    **client/.prettierrc:**
    - JSON: { "semi": false, "singleQuote": true, "trailingComma": "all" }

    **client/index.html:**
    - Standard Vite HTML template with div#root and script type="module" src="/src/main.jsx"

    **client/src/index.css:**
    - CSS reset (box-sizing border-box, margin 0, padding 0)
    - CSS custom properties: --color-primary, --color-bg, --color-surface, --color-text, --color-muted
    - Body font-family: system-ui, sans-serif

    **client/src/setupTests.js:**
    - Import '@testing-library/jest-dom'

    **client/src/main.jsx:**
    - Import StrictMode, createRoot, createBrowserRouter, RouterProvider
    - Import App, Dashboard, Resume, NewApplication, Applications
    - Create router with createBrowserRouter: root path '/' element App, children: index Dashboard, 'resume' Resume, 'new' NewApplication, 'applications' Applications
    - Render RouterProvider with StrictMode

    **client/src/App.jsx:**
    - Import Outlet from react-router-dom
    - Import Navbar component
    - Import styles from App.module.css
    - Render div.app wrapping Navbar and main.main with Outlet inside

    **client/src/App.module.css:**
    - .app: max-width 1200px, margin 0 auto, padding 0 1rem
    - .main: padding 2rem 0

    **client/src/components/Navbar/Navbar.jsx:**
    - Import NavLink from react-router-dom
    - Import styles from Navbar.module.css
    - Render nav with navLinks array: [{to:'/', label:'Dashboard'}, {to:'/resume', label:'Resume'}, {to:'/new', label:'New Application'}, {to:'/applications', label:'Applications'}]
    - Map navLinks to NavLink components with className callback for active styling
    - Use end prop on Dashboard NavLink (index route)

    **client/src/components/Navbar/Navbar.module.css:**
    - .navbar: display flex, gap 1.5rem, padding 1rem 0, border-bottom 1px solid #e0e0e0
    - .link: text-decoration none, color var(--color-muted), font-weight 500
    - .link.active: color var(--color-primary), border-bottom 2px solid var(--color-primary)

    **Placeholder pages (client/src/pages/):**
    - Each page: simple component with h1 title and brief description text
    - Dashboard.jsx: h1 "Dashboard", p "Overview of your job application activity"
    - Resume.jsx: h1 "Resume", p "View and edit your resume content"
    - NewApplication.jsx: h1 "New Application", p "Create a new job application"
    - Applications.jsx: h1 "Applications", p "Track your job applications"

    **After creating all files:**
    - Run npm install in root, client, and server directories
    - Verify `npm run dev` starts both Vite (port 5173) and Express (port 3000)
    - Verify http://localhost:5173 loads in browser
    - Verify clicking nav links navigates between all four pages
  </action>
  <verify>
    <automated>
      cd /Users/kmpg/VibeCodeTours/applytrail/client && npx vite &
      sleep 3
      curl -s http://localhost:5173 | grep -q 'root' && echo "Vite OK" || echo "Vite FAIL"
      kill %1 2>/dev/null
    </automated>
  </verify>
  <done>
    - `cd client && npx vite` starts Vite dev server on port 5173
    - Browser at localhost:5173 shows the Dashboard page
    - Clicking nav links navigates to Resume, New Application, and Applications pages without page reload
    - ESLint runs without errors: `cd client && npm run lint`
  </done>
</task>

<task type="auto">
  <name>Task 2: Resume Data — Migrate resume.md to resume.json</name>
  <files>
    resume.json
  </files>
  <action>
    Migrate the existing resume.md content into a structured resume.json file in the project root, per D-07 (resume as structured JSON).

    Read the existing resume.md at the project root. Extract all sections into a JSON structure:

    **resume.json structure:**
    - name: "Alex Tan" (from H1)
    - contact: { email, github, location } parsed from the line under the name
    - summary: the paragraph under ## Summary
    - experience: array of objects, each with company, role, period, bullets array
      - Parse "**Software Developer — Brightlane Solutions** (Jan 2024 – Present)" into company "Brightlane Solutions", role "Software Developer", period "Jan 2024 – Present"
      - Each bullet point becomes a string in the bullets array
    - projects: array of objects, each with name, description, bullets array
      - Parse "**Habit Tracker Web App** — Personal project, 2025" into name and description
    - skills: array of strings, split by comma from the Skills section
    - education: array of objects with degree, school, year
      - Parse "**B.Sc. Computer Science** — Multimedia University (MMU), Malaysia — Graduated 2022"

    **Important:** Preserve ALL content from resume.md — every bullet, every skill, every detail. This is a format migration, not a content edit.

    Keep resume.md as-is (do not delete it).

    **After creating resume.json:**
    - Verify the JSON is valid: `node -e "JSON.parse(require('fs').readFileSync('resume.json','utf-8'))"`
    - Spot-check: name is "Alex Tan", 2 experience entries, 2 project entries, 13 skills, 1 education entry
  </action>
  <verify>
    <automated>
      cd /Users/kmpg/VibeCodeTours/applytrail && node -e "const r=JSON.parse(require('fs').readFileSync('resume.json','utf-8')); assert(r.name==='Alex Tan'); assert(r.experience.length===2); assert(r.skills.length>=10); console.log('Resume JSON OK')"
    </automated>
  </verify>
  <done>
    - resume.json exists at project root with valid JSON
    - name is "Alex Tan"
    - experience array has 2 entries (Brightlane Solutions, Tigerwave Apps)
    - projects array has 2 entries (Habit Tracker, Repo Health CLI)
    - skills array has all skills from resume.md
    - education array has 1 entry
  </done>
</task>

<task type="auto">
  <name>Task 3: Data Persistence — Express API serving JSON files</name>
  <files>
    server/package.json,
    server/index.js
  </files>
  <action>
    Create the Express server with API routes that read and write JSON files from the project root, per D-01 (monorepo layout), D-04 (minimal Express with express.json()), D-05 (JSON data in project root), D-08 (RESTful routes).

    **server/package.json:**
    - Name: "applytrail-server", private: true
    - Main: "index.js"
    - Scripts: "start": "node index.js"
    - Dependencies: express

    **server/index.js:**
    - CommonJS (require syntax) — no "type": "module" in server/package.json
    - Import express, fs, path
    - Create app with express.json() middleware (per D-04 — no extra middleware)
    - const DATA_DIR = path.join(__dirname, '..') to reach project root (per D-05)
    - Helper readJSON(filename): join DATA_DIR + filename, check existsSync, parse JSON, return array or empty array
    - Helper writeJSON(filename, data): writeFileSync with JSON.stringify(data, null, 2) + '\n'
    - GET /api/applications: res.json(readJSON('applications.json'))
    - GET /api/resume: res.json(readJSON('resume.json'))
    - PUT /api/resume: writeJSON('resume.json', req.body), res.json({ ok: true })
    - Listen on process.env.PORT || 3000

    **After creating server/index.js:**
    - Run npm install in server/
    - Verify Express starts on port 3000
    - Verify GET /api/applications returns the 5 existing application entries
    - Verify GET /api/resume returns resume.json data
    - Verify PUT /api/resume persists data (write, restart server, read back)
  </action>
  <verify>
    <automated>
      cd /Users/kmpg/VibeCodeTours/applytrail/server && node index.js &
      sleep 2
      curl -s http://localhost:3000/api/applications | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const j=JSON.parse(d);if(j.length!==5)throw new Error('Expected 5, got '+j.length);console.log('Applications OK')})"
      curl -s http://localhost:3000/api/resume | node -e "let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const j=JSON.parse(d);if(j.name!=='Alex Tan')throw new Error('Name mismatch');console.log('Resume OK')})"
      kill %1 2>/dev/null
    </automated>
  </verify>
  <done>
    - GET /api/applications returns 5 entries matching existing applications.json
    - GET /api/resume returns resume.json content
    - PUT /api/resume writes data that persists after server restart
    - Vite proxy at localhost:5173/api/applications returns same data as direct Express call
  </done>
</task>

</tasks>

<verification>
Phase-level verification checks:

1. **Single-command startup:** `npm run dev` from project root starts both Vite (5173) and Express (3000)
2. **Navigation works:** Browser at localhost:5173 shows Dashboard; clicking nav links loads Resume, New Application, Applications pages
3. **API serves data:** `curl http://localhost:3000/api/applications` returns 5 entries; `curl http://localhost:3000/api/resume` returns resume.json
4. **Proxy works:** `curl http://localhost:5173/api/applications` returns same data as direct Express call
5. **Data persists:** Stop server, restart, `curl /api/applications` still returns 5 entries
6. **Lint passes:** `cd client && npm run lint` exits cleanly
7. **Tests run:** `cd client && npm run test -- --run` exits cleanly (even if no tests yet)
</verification>

<success_criteria>
1. `npm run dev` starts the app and loads in the browser at localhost:5173
2. User can navigate between Dashboard, Resume, New Application, and Applications via the nav bar
3. GET /api/applications returns the 5 existing application entries from applications.json
4. GET /api/resume returns structured resume data from resume.json
5. Data persists across server restarts (JSON files are read from disk)
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
