---
phase: 02-resume-job-input
plan: 04
type: execute
wave: 1
depends_on:
  - 02-01
files_modified:
  - client/src/pages/NewApplication.jsx
  - client/src/pages/NewApplication.module.css
  - server/index.js
  - job_postings.json
autonomous: true
requirements:
  - JOB-01
user_setup: []

must_haves:
  truths:
    - "NewApplication page has a form with company, role, and job posting text fields"
    - "Submitting the form sends POST /api/job-postings"
    - "Form clears after successful save"
    - "Success message appears after saving"
    - "GET /api/job-postings returns saved postings"
    - "job_postings.json persists data on disk"
  artifacts:
    - "client/src/pages/NewApplication.jsx (job posting input form)"
    - "client/src/pages/NewApplication.module.css (form styles)"
    - "server/index.js (GET/POST /api/job-postings routes)"
    - "job_postings.json (persisted job posting data)"
  key_links:
    - "POST /api/job-postings creates { id, company, role, posting_text, created_at }"
    - "GET /api/job-postings returns array of saved postings"
    - "Form clears on success, shows error on failure"
    - "readJSON/writeJSON helpers handle job_postings.json"
---

<objective>
Build the job posting input form and API routes so users can paste job posting text and save it for later use. This is the second core input for cover letter generation in Phase 3.

Purpose: Capture job posting data (company, role, posting text) in the web app so it can feed into the cover letter generator.

Output: A functional New Application page at /new with a form that persists job postings to job_postings.json via API.
</objective>

<execution_context>
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/workflows/execute-plan.md
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/index.js
@applications.json
@client/src/pages/NewApplication.jsx
@client/src/pages/Resume.module.css
@client/src/index.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 2.4: Job Posting Input — Form to capture and persist job postings</name>
  <files>
    client/src/pages/NewApplication.jsx,
    client/src/pages/NewApplication.module.css,
    server/index.js,
    job_postings.json
  </files>
  <read_first>
    server/index.js,
    client/src/pages/NewApplication.jsx,
    client/src/pages/Resume.module.css,
    client/src/index.css,
    applications.json
  </read_first>
  <behavior>
    - Test: NewApplication page renders a form with company name, role title, and job posting textarea
    - Test: Submitting the form sends POST /api/job-postings with { company, role, posting_text }
    - Test: After successful submission, the form clears and a success message appears
    - Test: GET /api/job-postings returns an array of saved job postings
    - Test: Each saved posting has id, company, role, posting_text, and created_at fields
    - Test: Error message appears if save fails
  </behavior>
  <action>
    Build the job posting input form and supporting API routes.

    **Server-side** (server/index.js):
    - Add GET /api/job-postings route: res.json(readJSON('job_postings.json'))
    - Add POST /api/job-postings route:
      - Read existing postings via readJSON('job_postings.json')
      - Create new posting: { id: Date.now().toString(), company: req.body.company, role: req.body.role, posting_text: req.body.posting_text, created_at: new Date().toISOString().split('T')[0] }
      - Append to array, writeJSON back
      - Return { ok: true, posting: newPosting }
    - Place new routes after existing /api/applications route, before /api/resume routes

    **Data file** (job_postings.json):
    - Initialize as empty array [] if it does not exist
    - Structure: array of { id, company, role, posting_text, created_at }

    **NewApplication page** (client/src/pages/NewApplication.jsx):
    - State: company (string), role (string), postingText (string), saving (boolean), message (string)
    - Render a form with:
      - Company Name: text input, required
      - Role Title: text input, required
      - Job Posting Text: textarea (12 rows), placeholder "Paste the job posting text here...", required
      - "Save Job Posting" button (submit)
    - On submit: POST /api/job-postings with { company, role, posting_text: postingText }
    - On success: clear form fields, show "Job posting saved!" message for 3 seconds
    - On error: show "Failed to save. Please try again." message
    - While saving: disable button, show "Saving..."

    **Styling** (client/src/pages/NewApplication.module.css):
    - .page: max-width 800px, margin 0 auto, padding 2rem
    - .form: display flex, flex-direction column, gap 1.5rem
    - .field: display flex, flex-direction column, gap 0.25rem
    - .label: font-weight 600, font-size 0.875rem, color var(--color-muted)
    - .input, .textarea: padding 0.5rem, border 1px solid #e0e0e0, border-radius 4px, font-family inherit, font-size 1rem
    - .textarea: resize vertical, min-height 200px
    - .submitButton: background var(--color-primary), color white, border none, padding 0.75rem 2rem, border-radius 4px, cursor pointer, font-size 1rem, font-weight 600, align-self flex-start
    - .submitButton:disabled: opacity 0.6, cursor not-allowed
    - .message: font-size 0.875rem, padding 0.5rem
    - .success: color #22c55e
    - .error: color #ef4444

    **After implementing:**
    - Navigate to /new — form should render
    - Fill in company, role, and paste text, click Save
    - Verify job_postings.json contains the new entry
    - Verify form clears after save and shows success message
  </action>
  <verify>
    <automated>
      cd /Users/kmpg/VibeCodeTours/applytrail && node -e "
        const http = require('http');

        // Clean up any previous test data
        const fs = require('fs');
        let postings = [];
        try { postings = JSON.parse(fs.readFileSync('job_postings.json', 'utf-8')); } catch {}
        postings = postings.filter(p => p.company !== 'Test Corp');
        fs.writeFileSync('job_postings.json', JSON.stringify(postings, null, 2) + '\n');

        // Step 1: Verify POST /api/job-postings
        const postData = JSON.stringify({ company: 'Test Corp', role: 'Developer', posting_text: 'We are looking for a developer...' });
        const postReq = http.request({ hostname: 'localhost', port: 3000, path: '/api/job-postings', method: 'POST', headers: { 'Content-Type': 'application/json', 'Content-Length': Buffer.byteLength(postData) } }, (res) => {
          let d = '';
          res.on('data', c => d += c);
          res.on('end', () => {
            const r = JSON.parse(d);
            if (!r.ok || !r.posting) throw new Error('POST failed: ' + d);
            if (!r.posting.id || !r.posting.created_at) throw new Error('Missing id or created_at');
            console.log('POST /api/job-postings OK: id=' + r.posting.id + ', created_at=' + r.posting.created_at);

            // Step 2: Verify GET /api/job-postings
            http.get('http://localhost:3000/api/job-postings', (res2) => {
              let d2 = '';
              res2.on('data', c => d2 += c);
              res2.on('end', () => {
                const all = JSON.parse(d2);
                if (!Array.isArray(all)) throw new Error('GET did not return array');
                const found = all.find(p => p.company === 'Test Corp');
                if (!found) throw new Error('Test posting not found in GET');
                console.log('GET /api/job-postings OK: ' + all.length + ' posting(s)');

                // Clean up test data
                const cleaned = all.filter(p => p.company !== 'Test Corp');
                fs.writeFileSync('job_postings.json', JSON.stringify(cleaned, null, 2) + '\n');
                console.log('Test data cleaned up');
              });
            });
          });
        });
        postReq.write(postData);
        postReq.end();
      "
    </automated>
  </verify>
  <done>
    - NewApplication page at /new shows a form with company name, role title, and job posting textarea
    - Submitting the form sends POST /api/job-postings and data persists in job_postings.json
    - Form clears after successful save and shows a success message
    - Error message shows if save fails
    - GET /api/job-postings returns the array of saved job postings with id, company, role, posting_text, created_at
    - job_postings.json is a valid JSON array in the project root
    - CSS styling matches the app design system
  </done>
</task>

</tasks>

<verification>
Phase-level verification checks:

1. **Form renders:** Navigate to /new — form with 3 fields visible
2. **Save works:** Fill form, click Save — "Job posting saved!" appears
3. **Form clears:** After save, all fields are empty
4. **Data persists:** job_postings.json contains the saved entry
5. **API returns data:** curl GET /api/job-postings returns array with saved posting
6. **Entry structure:** Each posting has id, company, role, posting_text, created_at
</verification>

<success_criteria>
1. User can enter company, role, and job posting text in the form
2. Submitting saves to job_postings.json via POST /api/job-postings
3. Form clears after save with success message
4. GET /api/job-postings returns saved postings
5. Each posting has id, company, role, posting_text, created_at fields
6. Styling matches the app design system
</success_criteria>

<artifacts_this_phase_produces>
- client/src/pages/NewApplication.jsx — Job posting input form
- client/src/pages/NewApplication.module.css — Form styles
- server/index.js — Updated with GET/POST /api/job-postings routes
- job_postings.json — New data file for persisted job postings
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/02-resume-job-input/02-04-SUMMARY.md` when done
</output>
