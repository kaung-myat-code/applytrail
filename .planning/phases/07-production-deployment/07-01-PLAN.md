---
phase: 07-production-deployment
plan: 01
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - render.yaml
  - package.json
  - server/index.js
autonomous: false
requirements:
  - PROD-01
  - PROD-02
  - PROD-03
  - PROD-04
must_haves:
  truths:
    - App is accessible at a public *.onrender.com URL
    - All features work end-to-end on the deployed instance
    - Health check endpoint returns successful response on deployed instance
    - Pushing to main branch triggers automatic redeployment
  artifacts:
    - render.yaml with correct web service configuration
    - Working build and start scripts in package.json
    - Health endpoint in server/index.js
  key_links:
    - render.yaml buildCommand produces working client build
    - render.yaml startCommand launches production server
    - server/index.js serves static files from client/dist/
---

<objective>
Deploy ApplyTrail to Render free tier and verify it is fully functional at a public URL.

Purpose: Make the app publicly accessible as a portfolio piece, completing the v1.1 Release Polish milestone.
Output: Live app at *.onrender.com with all features verified working.
</objective>

<execution_context>
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/workflows/execute-plan.md
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-production-deployment/CONTEXT.md
render.yaml
package.json
server/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight verification of deployment configuration</name>
  <files>render.yaml, package.json, server/index.js</files>
  <action>
    Verify all deployment prerequisites are met before Render setup (per DEPLOY-08, DEPLOY-06):

    1. Confirm render.yaml exists at project root with correct structure:
       - type: web, name: applytrail, runtime: node, plan: free
       - buildCommand: npm install && npm run build
       - startCommand: npm start
       - envVars includes NODE_ENV=production

    2. Confirm root package.json has required scripts:
       - build: runs client build (cd client && npm run build)
       - start: runs production server (cd server && node index.js)
       - postinstall: installs client and server dependencies

    3. Confirm server/index.js has production features:
       - helmet middleware (gated on NODE_ENV=production)
       - compression middleware (gated on NODE_ENV=production)
       - Static file serving from client/dist/
       - SPA catch-all route (res.sendFile for non-API routes)
       - Health endpoint at GET /api/health returning JSON

    4. Confirm demo data seeding is in place:
       - server/demo-data/ directory contains resume.json, job_postings.json, applications.json
       - seedDemoData() function is called on startup
       - Seeding only occurs when data files are missing or empty

    5. Run a local production build test to confirm build succeeds:
       - Run: npm install && npm run build
       - Verify client/dist/ directory is created with index.html
       - Run: NODE_ENV=production npm start
       - Verify: curl http://localhost:3000/api/health returns JSON with status "ok"
       - Stop the server after verification
  </action>
  <verify>
    <automated>cd /Users/kmpg/VibeCodeTours/applytrail && npm run build && NODE_ENV=production timeout 10 npm start &; sleep 3; curl -s http://localhost:3000/api/health | grep -q '"status":"ok"'; kill %1 2>/dev/null; echo "PASS"</automated>
  </verify>
  <done>
    - render.yaml has correct configuration
    - package.json has build, start, and postinstall scripts
    - Local production build succeeds and health endpoint responds
    - Demo data files exist in server/demo-data/
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Create Render web service and connect GitHub repo</name>
  <files>render.yaml</files>
  <action>
    Deploy the app to Render via the dashboard. This is a human-operational task with no CLI equivalent.

    Steps:
    1. Go to https://dashboard.render.com and sign in (or create a free account)
    2. Click "New" then "Blueprint" to use the render.yaml blueprint
    3. Connect GitHub account and select the applytrail repository
    4. Render detects render.yaml and auto-configures the web service
    5. Click "Apply" to create the service
    6. Wait for initial deploy to complete (2-5 minutes on first deploy)
    7. Note the assigned public URL (e.g., https://applytrail.onrender.com)
    8. Go to Settings then Build and Deploy, confirm Auto-Deploy is set to Yes for main branch

    If Blueprint is not available, create a manual Web Service:
    - Name: applytrail, Runtime: Node, Plan: Free
    - Build Command: npm install && npm run build
    - Start Command: npm start
    - Environment Variable: NODE_ENV = production
  </action>
  <verify>
    <automated>echo "Requires human to complete Render dashboard setup and provide URL"</automated>
  </verify>
  <done>Render web service is created, initial deploy succeeds, public URL is assigned, auto-deploy is enabled for main branch</done>
  <resume-signal>Provide the deployed *.onrender.com URL</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Verify deployed instance end-to-end</name>
  <files>render.yaml, server/index.js</files>
  <action>
    Verify the deployed Render instance is fully functional (per PROD-03, PROD-04).

    Use the URL provided by the human from Task 2 (e.g., https://applytrail.onrender.com).

    1. Health check verification (PROD-03):
       - Run: curl -s https://APPLYTRAIL_URL/api/health
       - Expect: JSON response with "status": "ok" and uptime field
       - If response is slow (30+ seconds), note that Render free tier spins down after 15 min inactivity

    2. API data verification:
       - Run: curl -s https://APPLYTRAIL_URL/api/resume
       - Expect: JSON object with demo resume data (Jordan Rivera)
       - Run: curl -s https://APPLYTRAIL_URL/api/job-postings
       - Expect: JSON array with 2-3 demo job postings
       - Run: curl -s https://APPLYTRAIL_URL/api/applications
       - Expect: JSON array with demo applications in various statuses

    3. Static file serving verification:
       - Run: curl -s -o /dev/null -w "%{http_code}" https://APPLYTRAIL_URL/
       - Expect: 200 (React app HTML served)
       - Run: curl -s -o /dev/null -w "%{http_code}" https://APPLYTRAIL_URL/resume
       - Expect: 200 (SPA catch-all serves index.html for client routes)

    4. Document results:
       - Record the public URL
       - Record any issues found
       - Note: Data resets on each redeploy (expected behavior for free tier with JSON storage)
  </action>
  <verify>
    <automated>echo "Manual verification required — provide deployed URL to run curl checks"</automated>
  </verify>
  <done>
    - Health endpoint returns JSON with status "ok" on deployed instance
    - API endpoints return demo data
    - Static files are served correctly (200 responses)
    - Public URL is documented
  </done>
</task>

</tasks>

<verification>
After all tasks complete, verify the full deployment checklist:

1. App accessible at *.onrender.com URL: curl returns 200
2. Health check works: curl /api/health returns JSON status "ok"
3. Demo data loads: curl /api/resume returns Jordan Rivera resume
4. All features work: browser test of resume editing, job posting, cover letter generation, application tracking
5. Auto-deploy configured: push a test commit and verify Render rebuilds
</verification>

<success_criteria>
- App is live at a public *.onrender.com URL
- GET /api/health returns JSON with status "ok"
- All four features work end-to-end in the browser (resume editing, job posting input, cover letter generation, application tracking)
- Pushing to main branch triggers automatic redeployment on Render
</success_criteria>

<output>
Create `.planning/phases/07-production-deployment/07-01-SUMMARY.md` when done
</output>
