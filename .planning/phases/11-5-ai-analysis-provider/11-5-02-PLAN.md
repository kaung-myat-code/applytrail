---
phase: 11-5-ai-analysis-provider
plan: 02
type: execute
wave: 2
depends_on: [11-5-01]
files_modified:
  - client/src/pages/Analysis.jsx
  - client/src/pages/Analysis.module.css
autonomous: false
requirements: [AI-ANALYSIS-01, AI-ANALYSIS-02, AI-ANALYSIS-04]
must_haves:
  truths:
    - Provider dropdown appears on Analysis page between posting selector and analyze button
    - Dropdown defaults to heuristic
    - AI option label communicates that an API key is needed
    - Fallback banner appears when AI analysis falls back to heuristic
    - Provider value is sent in the POST /api/analyze request body
    - Existing report display (ScoreDisplay, KeywordGroups, SectionFindings) renders AI results without changes
  artifacts:
    - client/src/pages/Analysis.jsx (updated)
    - client/src/pages/Analysis.module.css (updated)
  key_links:
    - provider state variable sent in fetch body as provider field
    - data.fallback flag from API response triggers banner display
    - fallback banner is dismissible or auto-clears on next analysis
---

<objective>
Add provider selection UI to the Analysis page and handle fallback display so users can choose between heuristic and AI analysis with clear feedback.

Purpose: User-facing control for the AI provider backend built in plan 01.

Output: Provider dropdown on Analysis page, fallback banner styling, provider sent to API.
</objective>

<execution_context>
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/workflows/execute-plan.md
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-5-ai-analysis-provider/11-5-01-PLAN.md
@client/src/pages/Analysis.jsx
@client/src/pages/Analysis.module.css
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add provider selector dropdown and fallback banner to Analysis page</name>
  <files>client/src/pages/Analysis.jsx, client/src/pages/Analysis.module.css</files>
  <behavior>
    - Page renders a select element with id="provider" containing two options: heuristic and ai
    - The ai option text includes a note about API key requirement
    - Default selected value is 'heuristic'
    - The provider value is included in the POST /api/analyze request body
    - When API response contains fallback: true, a warning banner appears above the report
    - Banner text says AI analysis fell back to heuristic
    - Banner clears when user runs a new analysis
  </behavior>
  <action>
**Analysis.jsx changes:**

1. Add two new state variables after the existing ones:
   - `const [provider, setProvider] = useState('heuristic')`
   - `const [fallbackInfo, setFallbackInfo] = useState(null)` -- stores { reason } when fallback occurs

2. In handleAnalyze, clear fallbackInfo at the start: `setFallbackInfo(null)`

3. Update the fetch body to include provider:
   ```
   body: JSON.stringify({
     job_posting_id: selectedPostingId,
     resume_version_id: selectedResumeId || undefined,
     provider,
   }),
   ```

4. After parsing the response data, check for fallback:
   ```
   if (data.fallback) {
     setFallbackInfo({ reason: data.fallback_reason || 'AI provider unavailable' })
   }
   ```
   Also update the suggestions state from data.suggestions (this already happens).

5. Add the provider selector field in the form, between the Job Posting select and the Analyze button. Use the existing `.field`, `.label`, `.select` CSS classes:
   ```
   <div className={styles.field}>
     <label className={styles.label} htmlFor="provider">Analysis Provider</label>
     <select
       className={styles.select}
       id="provider"
       value={provider}
       onChange={e => setProvider(e.target.value)}
     >
       <option value="heuristic">Heuristic (Keyword Match) -- no setup needed</option>
       <option value="ai">AI Analysis (Gemini) -- requires API key</option>
     </select>
   </div>
   ```

6. Add fallback banner above the report section (after the form, before `{report && ...}`):
   ```
   {fallbackInfo && (
     <div className={styles.fallbackBanner}>
       <strong>AI analysis unavailable</strong> -- falling back to heuristic analysis.
       <span className={styles.fallbackReason}>{fallbackInfo.reason}</span>
     </div>
   )}
   ```

7. The existing report rendering (ScoreDisplay, KeywordGroups, SectionFindings, Review Suggestions link) stays unchanged -- AI-generated reports have the same shape as heuristic reports.

**Analysis.module.css changes:**

Add fallback banner styles after the `.error` block:
```
.fallbackBanner {
  margin-top: var(--space-4);
  padding: var(--space-3) var(--space-4);
  background: var(--color-warning-bg);
  border: 1px solid var(--color-warning);
  border-radius: var(--radius-sm);
  color: var(--color-text);
  font-size: 0.9rem;
  line-height: 1.5;
}

.fallbackReason {
  display: block;
  font-size: 0.8rem;
  color: var(--color-muted);
  margin-top: var(--space-1);
}
```

Use existing CSS variables (--color-warning-bg, --color-warning) for consistency with the design system. Check client/src/index.css to confirm these variables exist; if not, use a fallback like `#fff3cd` for background and `#856404` for border.

Do NOT change the ScoreDisplay, KeywordGroups, or SectionFindings components -- they already render the MatchReport shape correctly regardless of provider.
  </action>
  <verify>
    <automated>cd /Users/kmpg/VibeCodeTours/applytrail/client && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Provider dropdown renders on Analysis page. Fallback banner appears when API response has fallback: true. Provider value sent in API request. Frontend builds without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify AI Analysis Provider end-to-end</name>
  <what-built>AI Analysis Provider feature: provider selector on Analysis page, AI-powered analysis via Gemini, automatic fallback to heuristic with banner notification.</what-built>
  <how-to-verify>
    1. Start the dev server: `cd /Users/kmpg/VibeCodeTours/applytrail && npm run dev`
    2. Open http://localhost:5173 and navigate to the Analysis page
    3. Verify the provider dropdown appears between the Job Posting selector and the Analyze button
    4. Verify "Heuristic (Keyword Match)" is selected by default
    5. Run an analysis with heuristic provider -- confirm it works as before (score, keywords, sections, suggestions)
    6. Switch to "AI Analysis (Gemini)" and run analysis WITHOUT setting GOOGLE_GENERATIVE_AI_API_KEY
    7. Verify: a yellow fallback banner appears saying "AI analysis unavailable -- falling back to heuristic analysis"
    8. Verify: the report still shows heuristic results below the banner
    9. If you have a GOOGLE_GENERATIVE_AI_API_KEY: set it in server/.env, restart server, switch to AI provider, run analysis
    10. Verify: AI-powered results appear (different from heuristic, more detailed section summaries)
    11. Click "Review Suggestions" link -- verify it works with AI-generated suggestions
    12. Switch back to heuristic and run again -- verify no regression
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
  <action>Follow the verification steps above to confirm the full AI Analysis Provider feature works end-to-end: provider dropdown renders, heuristic works unchanged, AI fallback shows banner, and Review Suggestions works with both providers.</action>
  <verify>
    <human-check>All 12 verification steps pass</human-check>
  </verify>
  <done>Provider selector, fallback banner, and AI analysis all work correctly. Heuristic provider has no regression. Review Suggestions works with AI-generated suggestions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | Provider name sent in request body; server validates |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11.5-01 | Information Disclosure | AI provider sends resume+posting to Gemini API | medium | mitigate | User explicitly opts in via dropdown; documented in option label |
| T-11.5-04 | Credential Leak | API key visible in fallback banner | low | mitigate | fallback_reason from API is sanitized server-side; banner shows generic message |
</threat_model>

<verification>
Full end-to-end verification:

1. `cd /Users/kmpg/VibeCodeTours/applytrail/client && npx vite build` completes without errors
2. Provider dropdown present in rendered HTML with two options
3. Heuristic analysis works unchanged
4. AI analysis without key shows fallback banner with heuristic results
5. AI analysis with key shows AI-powered results
6. Review Suggestions page works with both provider outputs
</verification>

<success_criteria>
- [ ] Provider dropdown renders on Analysis page with heuristic default
- [ ] Fallback banner appears when AI analysis falls back to heuristic
- [ ] Provider value is sent in API request body
- [ ] Heuristic provider works identically (no regression)
- [ ] Review Suggestions works with AI-generated suggestions
- [ ] Frontend builds cleanly
</success_criteria>

<output>
Create `.planning/phases/11-5-ai-analysis-provider/11-5-02-SUMMARY.md` when done
</output>
