---
phase: quick
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/index.js
autonomous: true
requirements: []
must_haves:
  truths:
    - fallback_reason message references the user-selected provider's error, not the last tried provider's error
  artifacts:
    - server/index.js with fixed fallback error capture
  key_links: []
---

<objective>
Fix the fallback error message in POST /api/analyze to show the correct provider's error instead of the last tried provider's error.

Purpose: When all AI providers fail and the system falls back to heuristic, the user sees a misleading error message (e.g. "Groq API key is missing" when they selected Gemini). This fix ensures the fallback_reason references the user-selected provider's error.

Output: Fixed fallback chain in server/index.js that captures the selected provider's error separately.
</objective>

<execution_context>
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/workflows/execute-plan.md
@/Users/kmpg/VibeCodeTours/applytrail/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@server/index.js
@server/lib/analysis/providers/ai.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix fallback error capture to use selected provider's error</name>
  <files>server/index.js</files>
  <action>In server/index.js lines 470-500, fix the fallback chain error capture in POST /api/analyze. The current code overwrites `lastError` on each iteration (line 490), so `sanitizeError(lastError)` on line 500 always shows the last tried provider's error. Fix by: (1) After the `const fallbackOrder = [...]` line (~474), capture the selected provider's error separately with `let selectedProviderError`. (2) Inside the catch block (~489-493), add a check: if `aiProvider === providerName` (the user-selected provider), set `selectedProviderError = err`. Keep the existing `lastError = err` assignment as-is. (3) On line 500, change `sanitizeError(lastError || new Error('Unknown error'))` to `sanitizeError(selectedProviderError || lastError || new Error('Unknown error'))`. This ensures when all providers fail, the message references the user-selected provider's error (e.g. missing API key for that provider), not the last tried fallback provider's error.</action>
  <verify>
    <automated>cd /Users/kmpg/VibeCodeTours/applytrail && grep -n 'selectedProviderError' server/index.js | head -5</automated>
  </verify>
  <done>fallback_reason message shows the correct provider's error when all AI providers fail</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Bug fix only, no new trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | N/A | No security impact from this bug fix |
</threat_model>

<verification>
- `grep -n 'selectedProviderError' server/index.js` shows the new variable used in fallback chain
- Fallback order logic is unchanged (same iteration order, same try/catch structure)
- `sanitizeError` call uses `selectedProviderError` instead of `lastError`
</verification>

<success_criteria>
When user selects Gemini and all AI providers fail, fallback_reason references GOOGLE_GENERATIVE_AI_API_KEY (not GROQ_API_KEY). Same for OpenRouter (OPENROUTER_API_KEY) and Groq (GROQ_API_KEY).
</success_criteria>

<output>
Create `.planning/quick/260704-3ys-fix-fallback-error-message-showing-wrong/260704-3ys-01-SUMMARY.md` when done
</output>
