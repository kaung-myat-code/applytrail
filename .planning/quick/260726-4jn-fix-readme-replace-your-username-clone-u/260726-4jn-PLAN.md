---
phase: quick-260726-4jn
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "The clone command in Getting started uses the real repo owner, not a placeholder"
    - "A first-person 'Why I built this' section appears above 'How it works', explaining the project's origin in 2-4 sentences"
    - "The top badge row shows a live CI status badge linking to the GitHub Actions workflow run"
  artifacts:
    - README.md
  key_links:
    - "CI badge image URL and link URL both resolve to kaung-myat-code/applytrail's actions/workflows/ci.yml, matching the 'CI' workflow name in .github/workflows/ci.yml"
---

<objective>
Fix three small accuracy/polish gaps in README.md: a placeholder clone URL, a missing "why this exists" narrative section, and a missing CI status badge.

Purpose: The clone command currently can't be copy-pasted correctly (YOUR_USERNAME placeholder), and the README lacks both the personal motivation behind the project and a visible build-status signal -- small trust/credibility signals for anyone landing on the repo.
Output: Updated README.md with corrected clone URL, new "Why I built this" section, and a CI badge in the top badge row.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Fix placeholder clone URL</name>
  <files>README.md</files>
  <action>
In the "Getting started" section, the git clone line currently reads "git clone https://github.com/YOUR_USERNAME/applytrail.git" (line 85). Replace the placeholder segment with the actual GitHub owner, kaung-myat-code, so the line reads "git clone https://github.com/kaung-myat-code/applytrail.git". Do not touch any other text in the file.
  </action>
  <verify>
    <automated>cd /Users/kmpg/VibeCodeTours/applytrail && grep -c "YOUR_USERNAME" README.md | grep -qx "0" && grep -c "git clone https://github.com/kaung-myat-code/applytrail.git" README.md | grep -qx "1" && echo PASS</automated>
  </verify>
  <done>README.md has no remaining placeholder-username text, and the clone command points at kaung-myat-code/applytrail.</done>
</task>

<task type="auto">
  <name>Task 2: Add "Why I built this" section</name>
  <files>README.md</files>
  <action>
Insert a new "## Why I built this" section between the divider line at line 64 and the existing "## How it works" heading at line 66, following the same pattern used elsewhere in the file (a horizontal-rule divider line, blank line, heading, blank line, body paragraph, blank line, then another divider line before the next heading). Write the body in first person, 2-4 sentences, matching the README's existing concise no-fluff tone: explain that the project grew out of the author's own job search -- the tedium of tracking applications and rewriting cover letters by hand -- and the desire for a local, no-login tool that keeps resume versions, postings, and application status together without sending data to a third party. Do not add marketing language or restate content already covered in the "Every application has a trail" blurb or the "How it works" numbered list.
  </action>
  <verify>
    <automated>cd /Users/kmpg/VibeCodeTours/applytrail && grep -c "^## Why I built this$" README.md | grep -qx "1" && [ "$(grep -n "^## Why I built this$" README.md | cut -d: -f1)" -lt "$(grep -n "^## How it works$" README.md | cut -d: -f1)" ] && echo PASS</automated>
  </verify>
  <done>A "## Why I built this" heading with a 2-4 sentence first-person body appears above "## How it works", bracketed by divider lines consistent with the rest of the README.</done>
</task>

<task type="auto">
  <name>Task 3: Add CI status badge</name>
  <files>README.md</files>
  <action>
In the centered badge row (currently lines 5-11, containing the React/Express/Node/Render/License badges), add one more badge for CI status, linked to the GitHub Actions workflow. The workflow file is .github/workflows/ci.yml with `name: CI`. Use the standard GitHub Actions badge format: image src "https://github.com/kaung-myat-code/applytrail/actions/workflows/ci.yml/badge.svg" with alt text "CI", wrapped in a link to "https://github.com/kaung-myat-code/applytrail/actions/workflows/ci.yml". Place it as the first badge in the row, before the React 18 badge, using the same `<a><img></a>` markup style as the existing badges.
  </action>
  <verify>
    <automated>cd /Users/kmpg/VibeCodeTours/applytrail && grep -c "actions/workflows/ci.yml/badge.svg" README.md | grep -qx "1" && grep -c "actions/workflows/ci.yml\"" README.md | grep -qx "1" && echo PASS</automated>
  </verify>
  <done>The top badge row includes a GitHub Actions CI badge that links to the repo's Actions workflow run for ci.yml.</done>
</task>

</tasks>

<verification>
Read the full README.md after all three tasks and confirm: (1) no "YOUR_USERNAME" text remains anywhere in the file, (2) the "Why I built this" section reads naturally in first person and sits directly above "How it works" with matching divider formatting, (3) the CI badge renders alongside the other badges using the same markup pattern, and (4) no other README content was altered.
</verification>

<success_criteria>
README.md clone command uses the real repo path, a "Why I built this" section exists above "How it works" in first person (2-4 sentences), and a CI badge linking to GitHub Actions is present in the top badge row -- all three verified via automated grep checks with no unrelated diffs.
</success_criteria>

<output>
Create `.planning/quick/260726-4jn-fix-readme-replace-your-username-clone-u/260726-4jn-SUMMARY.md` when done.
</output>
